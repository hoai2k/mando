# Build Progress

Tracked per milestone; updated as work lands. See `docs/PLAN.md` for full scope.

| # | Milestone | Status |
|---|---|---|
| 0 | Plan, asset request docs, project scaffold | ✅ done |
| 1 | Core systems: math, input (KB/M + 2 Xbox pads + menu nav), physics, audio synth | ✅ done |
| 2 | Animation system (canonical skeleton, procedural clips, layered animator) | ✅ done |
| 3 | Characters: playable Mandalorians + enemy builders (Tusken, Pyke, pirate ×2, droid, Nikto swoop, Imperials) | ✅ done |
| 4 | FX: particles, projectiles; twin-sun + nebula skies | ✅ done |
| 5 | Board 1: Tatooine (dunes, mesas, homestead, camp, sail barge, sarlacc) | ✅ done |
| 6 | Board 2: Waystation (12 platforms, spire, cranes, crates, freighter) | ✅ done |
| 7 | Player controller (run/jump/jetpack/dash/slam) + combat (blaster w/ aim assist, gaffi combo, rocket) + camera | ✅ done |
| 8 | Enemies: 4 AI styles (melee/ranged/swoop/hover) + 10-wave spawner | ✅ done |
| 9 | UI: HUD, gamepad-navigable menus, fullscreen button (bottom-right + View button) | ✅ done |
| 10 | 2-player split-screen co-op (horizontal split, per-player camera/HUD) | ✅ done |
| 11 | Build verified + game boots (headless Chromium screenshots), merged to main | ✅ done |
| 12 | Six new boards (Nevarro, Crevasse, Trask, Refinery, Great Forge, Ringworld) + board systems (hazard zones, movers, breakables, traction, light field) + 7 new enemy kinds | ✅ done |
| 13 | Water: swim/wade system, underwater rendering & stealth, Trask mamacore hunt, board 9 "The Prison Rig" (ocean world, electrified decks, explorable seafloor) | ✅ done |
| 14 | Pilotable vehicles (PLAN.md §17): swoop, speeder bike, landspeeder, skiff on five boards — RB mount/dismount, momentum driving, ramming, hull HP and shoot-downs | ✅ done |
| 14 | Four-player split-screen, a character select that grows with the room, controllers seated by use, asset warming + drop screen | ✅ done |
| 15 | Roster to nine playable fighters: four Mandalorians and five underworld hunters, each hunter with a signature weapon and voice; all original names | ✅ done |
| 16 | Every requested model delivered and integrated (playable hunters, ringworld enforcer, the three creatures — animated in code against their own rigs) | ✅ done |
| 17 | Board music: streamed playlists, per-board opening tracks, the whole map in one config file | ✅ done |
| 18 | Rideable banthas (PLAN.md §17): the first living mount — walks its own gait, charges on X, and leaves your gun hand free | ✅ done |

## Verified

- `tsc --noEmit` clean; `vite build` clean (main chunk ~211 KB / 69 KB gzip, plus Three's ~680 KB / 182 KB environment chunk).
- `npm test` drives the real build in Chromium behind a synthetic Xbox pad: title → territory → character select → a played wave, failing on any console error. It derives the roster it expects from the game (`__roster`), so a rename or a new fighter can't break it. CI runs it, plus the nine-board collision audit, before every deploy.
- Headless Chromium boot test: title → board select → all nine boards render and run combat; menus navigate by keyboard (same code path as gamepad d-pad events).
- Simulation stepping test: wave 1 spawns 5 enemies on Tatooine, enemies close from 25 m to melee and damage the player; kill scoring credits the right player; wave advance to wave 2 (7 enemies); blaster kills at range with aim assist; gaffi melee combo kills at close range; 2-player split screen renders both viewports with independent HUDs.

## Still open / next steps

- Real-hardware playtest & feel tuning (all movement/combat numbers are first-pass; the six new boards especially).
- Authored audio for the six newer boards — ambience loops, board effects, the two extra footstep surfaces and the enemy voice barks are still synth-only or silent (`docs/ASSETS_AUDIO.md`). Everything has a live hook; each is a file drop.
- Per-board music pairs for the seven boards that still share the two original playlists (`docs/ASSETS_AUDIO.md`). Four boards have a signature opener already.
- **Models: the second monster batch is open** (`docs/ASSETS_MODELS.md`, 2026-09-02): `sandworm`, `sandworm_arch`, `zillo`, `nexu`, `kwazel_maw`, with their reference canvases open in `ASSETS_IMAGES.md`. All five fight as procedural stand-ins today; a delivered file drops in with no code change. The troop carriers landed 2026-08-30, hours after being requested — the pre-wiring was the whole integration.
- **Models delivered: everything else.** The environment/prop set, the game-mode props and all six monster bosses are delivered and wired (`docs/ASSETS_MODELS.md`). Also open on the art side (`docs/ASSETS_IMAGES.md`): the environment prop reference sheets, the two carrier sheets, and the optional PvP drop-screen portraits.
- **Monster boss fights are at v1, on every territory.** Ten monsters, no creature serving two boards: nine finals (one per territory) plus the Dune Sea's worm as its champion. All spawn through the shared boss rhythm; the worm's burrow cycle is the one bespoke moveset shipped. The other movesets and the weak-zone hit volumes in `docs/BOSSES.md` §2 are the next round. The ledger of what is and is not implemented lives in that doc.
- Remaining small requests: three hunter drop-screen portraits and the six new runtime textures (each needs a one-line wiring change, noted per request).
- **Characters: nothing open.** Every requested character model is delivered and integrated, and so are all four signature-weapon props (`saber_curved`, `crossbow`, `longrifle`, `pistol`) — the parking decision that once held them is moot now the sculpts exist.
- Boss *fights* proper — the boss characters ship as late-wave elites, but phases, arenas and a boss bar are future work.

## Log

- 2026-08-27 — Plan written and merged to main. Asset request docs added. Scope addendum: Xbox controller everywhere, fullscreen button, split-screen co-op.
- 2026-08-27 — Full game implemented and verified: two boards, 8 enemy types, wave mode, split-screen co-op, controller-navigable menus, procedural audio. Merged to main.
- 2026-08-27 — Generated all 18 engine-consumed SFX with the ElevenLabs sound-generation API (`tools/generate-sfx.mjs`), checked in under `public/assets/audio/`; audio loader now accepts `.ogg` and `.mp3`. Verified every file decodes in Chromium. Docs updated.
- 2026-08-27 — Cast expansion merged: 5 playable Mandalorians (Boba Fett, Din Djarin, Bo-Katan, Paz Vizsla, The Armorer) with a controller-navigable character select; Imperial remnant enemies (stormtrooper, death trooper, flying dark trooper) join waves 6+; ally NPCs (The Marshal, IG-11, Fennec Shand) reinforce on waves 4/7/9; Grogu follows player 1 in his hover-pram. 23 more ElevenLabs sounds (footsteps, voice barks, ambience beds, music loops) with engine hooks. `docs/ASSETS_MODELS.md` lists all 22 characters needing authored 3D models. Example gameplay video captured from the real build (frame-stepped, ffmpeg-encoded).
- 2026-08-27 — Deployed to GitHub Pages at https://hoai2k.github.io/mando/ via `.github/workflows/deploy.yml` (builds and publishes on every push to `main`). Verified the deployed bundle boots and plays when served from the `/mando/` subpath, with all 41 audio files loading. Audio loader now probes `.mp3` before `.ogg` to avoid a 404 per sample; Grogu's pram angled so he's visible from the chase camera.
- 2026-08-27 — Fixed two player-reported bugs. (1) `Game.render` passed `domElement.width/height` (drawing-buffer pixels) to `setViewport`/`setScissor`, which scale by the renderer pixel ratio themselves — so devicePixelRatio was applied twice and the viewport overran the buffer on HiDPI displays, shoving the character up and to the right. Now uses `renderer.getSize()` (CSS pixels). Only reproduced at devicePixelRatio > 1, which is why earlier headless tests (dpr 1) missed it. (2) The camera-relative right vector was negated (`(cos, -sin)` instead of `forward x up = (-cos, sin)`), inverting strafe on both stick and keyboard and putting the chase camera over the left shoulder. Both call sites now share `yawBasis()` in `core/math.ts`. Grogu moved to the player's left so he no longer crowds the now-correct over-the-right-shoulder frame.
- 2026-08-27 — All 15 requested textures delivered and integrated. Both authored equirect panoramas now drive `scene.background` (procedural shader domes kept as fallback, hidden once a panorama loads); sand normal map on the terrain; neon sign, barrel props and title key art wired; `skyIntensity` added so the bright nebula doesn't silhouette the station platforms; HUD text shadows strengthened for the brighter skies. `tools/optimize-textures.mjs` re-encodes opaque maps to JPEG — 15.3 MB → 6.8 MB with no visible loss — and the texture loader now tries `.jpg` then `.png`. Delivered images and audio moved to `docs/ASSETS_COMPLETED.md`; `ASSETS_IMAGES.md` and `ASSETS_AUDIO.md` now list only open requests. Character A-pose turnaround sheets requested for all 22 characters (in `ASSETS_IMAGES.md`, referenced per character from `ASSETS_MODELS.md`).
- 2026-08-27 — Playtest feedback round. **Knockback** now reads: hits apply an impulse *and* a stagger window, because the AI's per-frame steering damp was erasing impulses before they showed (bolt ≈0.7 m, melee swing ≈2.7 m, finisher/explosion ≈5 m). **Blaster readability**: longer fatter near-white bolts with an additive halo at 75 m/s, muzzle flash, and shots now converge on the crosshair via a camera raycast or the soft-lock target instead of firing parallel from the muzzle — plus a bigger crosshair with a red lock ring. **Station falling** is a slow recoverable drift (12% gravity, 3.2 m/s terminal, fuel regen, no damage) rather than a plunge. **Massiff removed** entirely (code, audio, docs) so every enemy is human-size or larger. **Grogu** rides with Din Djarin only. **New sprint gauge**: hold B/Shift on the ground for ~14.4 m/s against a 6-second energy bar separate from jetpack fuel; the dash burst is now a tap of the same button in the air and costs energy.
- 2026-08-27 — Dialled the melee finisher back from a launch to a shove on playtest feedback: push 5.0 m → 3.1 m and lift 6.65 → 0.96 m/s, so targets slide clear along the ground instead of being flung (which on the station could throw them off a platform). `knockback()` gained a `lift` parameter; explosions keep their pop.
- 2026-08-27 — Investigated "incomplete bodies": `buildBiped` meshed the hips and chest bones but nothing on the `spine` between them, leaving a ~0.28 m hole at the waist (measured 1.08–1.36 m above the feet). It only showed on characters without a robe or long coat to hide it, which is why some looked fine — Tuskens and Pykes were covered, armoured ones were not. Added a proportional abdomen so it closes for every species. The same central-column scan caught a second defect: the Nikto swoop rider sat 0.9 m above its bike with nothing bridging the gap; the rider is now seated on the saddle. Playable roster cut to Din Djarin and Paz Vizsla ahead of authored models for both; Boba, Bo-Katan and The Armorer are shelved (one roster entry each to restore). Grogu removed from the game, audio and docs.
- 2026-08-27 — Game renamed **Daimyo → Mando**, and *Book of Boba Fett* framing dropped in favour of the Mandalorians. Title screen, browser title, README, `CLAUDE.md` and `docs/PLAN.md` retitled; the "Daimyo" victory/defeat banners are now "This is the Way" / "The Mando has fallen". `PLAN.md`'s player row replaced with Din Djarin and Paz Vizsla, and the Boba-specific Tusken framing removed. Boba Fett turnaround sheets deleted from `reference/characters/`; `ASSETS_MODELS.md` no longer names him among the shelved Mandalorians. No gameplay code changed — the playable roster was already Din + Paz. Entries above are left as the historical record.
- 2026-08-27 — Hostiles are now **posted around the board** instead of converging on the player. `spawner.ts` splits each wave into 2–4 strong squads and places them with farthest-point sampling (never within 55 m of a player, extra squads ringed around reused posts on solid ground), so clearing a wave means exploring and finding them. Enemies gained awareness states (`idle` → `alerted` → `engaged`) in `enemy.ts`: they hold their post until they see you — sight is per-kind range, halved to the sides, 8 m behind, and needs line of sight — or hear something. Noise carries: player fire 55 m, enemy fire 30 m, explosions 70 m (straight to combat), and alerts spread through a squad, so firefights pull the neighbours in. A new `director.ts` decides who may push: only 2 melee and 2 ranged commit per player at a time while the rest hold assigned bearings around you and shoot, which turns a mob into a firing line with flanks. Waves are bigger (≈9 on wave 1 to ≈41 on wave 10, ×1.5 in co-op), a wave that drags past 80 s makes the remnant sweep for you, and allies now escort the player rather than chasing the nearest hostile across the map. New `ui/radar.ts` draws a 120 m motion-tracker dial per viewport — contacts coloured by awareness, off-sweep contacts pinned to the rim as bearing chevrons, remaining-hostile count underneath (also spelled out in the wave banner and HUD line). Verified headless: posted squads drift <4 m while unaware, a camp at 16 m notices within a second, commitment caps hold at 2 melee under fire, and 41 fully-engaged hostiles cost 0.31 ms/update.
- 2026-08-27 — Authored Din Djarin and Paz Vizsla models integrated, the first real test of the swap contract. The supplied Rigify exports are flat skeletons with an A-pose rest and no clips, so `src/characters/authored.ts` rebuilds them into our hierarchy (preserving world rest transforms so the skin's bind matrices stay valid) and the procedural rig drives them per frame. Three things had to be right. **Retarget in world space**: our clips rest at all-identity rotations, so a bone's accumulated locals are already a world-space delta, and each authored bone takes `delta * restWorld` converted back against its own parent — which is what lets the two skeletons disagree about hierarchy (our shoulders parent the arms, theirs hang off the spine) and about rest pose. **Mirror the source pose**: our rig labels its `L` bones on +X, which for a character facing +Z is the *right* side, while Rigify's `L` really is left; mirroring through X=0 lines the sides up and puts the blaster in the model's right hand. **Light the metal**: these skins drive most of the armour to full metal, and metal with nothing to reflect renders black — which is exactly how they first looked — so the board's sky is now pre-filtered into a scene reflection probe. Verified pose by pose (idle, run, aim, flight, melee, death) against the procedural build, and bone-by-bone against measured world positions.
- 2026-08-27 — Added the **model workbench** at `/workbench/?edit=models`: an orbit turntable that runs any game clip on any character, with the authored model and the procedural build side by side. It shares the game's rig, clips and animator wholesale, so there is no second animation path to drift out of sync. Also made asset paths page-relative (`ASSET_ROOT`) so the second page reaches the same files, and added the workbench to the Vite build as a second entry point.
- 2026-08-27 — RDR2-styled combat feel, researched against how Red Dead Redemption 2 actually plays (lock-on modes, Dead Eye, Euphoria hit reactions, NPC self-preservation). **Gunplay**: aim-press now snaps the camera onto the target nearest the reticle (~0.15 s pull, then fine aim at 0.55× sensitivity), hip fire has base spread plus movement spread and per-shot recoil climb (halved when aimed). **Dead Eye** (`V` / R3): world drops to 0.3× on a smoothed ramp while the player's fire rate, cooldowns and camera stay on the wall clock; Dead Eye shots fly true and hit for 55; 6-second meter, +25% per kill, slow trickle otherwise; sepia canvas tint + glowing fourth HUD bar; shared world clock in split-screen. **Hit reactions**: a real knockdown state (explosions, ground slam, gaffi finisher put grounded humanoids flat for ~1.6–2.1 s, then they get up shaken), hits on downed/wounded enemies land double, killing blows fling with force scaled to the hit; non-droid humanoids left under 25% HP by a hit have a 40% chance to collapse into a wounded crawl that bleeds out in 8–12 s unless finished. **Self-preservation**: suppression from hits and near misses (bolts landing within 4.5 m) pins shooters — they stop advancing, fire slower and wilder until it decays; a death spikes suppression for everyone within 12 m; the last survivor of a 3+ squad has even odds per director tick of breaking and running, rallying ~55 m out as a new post. All verified headless (timescale 0.30 with enemies moving at ~0.35× real speed, knockdown up-at-2 s, double damage on the floor, pinned shooter closing 0.4 m in 1.5 s vs 13.7 m after recovery, 3/3 last survivors breaking) and in-browser (tint, meter, toggle, no console errors).
- 2026-08-27 — Playtest round. **Station wave stall fixed**: the wave-clear check tested `enemies.length > 0` as a stand-in for "the wave has started", but hostiles knocked into the abyss are removed the same frame they die rather than lingering as a corpse, so on the station the array could empty completely and the check could never fire again — 0 hostiles remaining, wave 1 forever. It now counts what the wave actually spawned. Reproduced the stall against the old guard and confirmed the fix advances to wave 2. **Low gravity on the station**: `Board.gravity` scales gravity for players and enemies alike; the waystation runs at 0.45, so a 10 m/s kick now carries 4.19 m instead of 1.92 m. The void drift below `voidY` is unchanged in practice, being terminal-speed limited. **Arms spread**: the rig's `L` bones sit at +X and a positive Z rotation swings a downward bone toward +X, so every clip's arm splay was signed the wrong way and tucked the arms into the torso — invisible on a stick figure, clipping straight through an authored model. Flipped the sign on the locomotion clips (idle, run, air, fly, hit, death) and widened idle to 14° and the run swing to 12°; the two-handed grips (aim, melee, enemy aim/swing) keep their authored values, since the off-hand there is meant to come across to the weapon. **Audio**: volumes moved into `src/config.ts` — SFX had been running at full bus gain, over the score, and now sits at 0.45 against music 0.4 under a 0.8 master. `audio.setSfxVolume/setMusicVolume/setMasterVolume` apply at runtime and `saveAudioConfig()` persists to localStorage, restored on boot.
- 2026-08-27 — **Hero ambient**: the player was getting lost against the station, whose cold key, absent sky bounce and nebula reflection probe leave beskar reading as a silhouette. `Board.heroLight` now lifts the player character alone — 0.34 on the station, 0.10 on Tatooine. It is not a light: Three tests a light's layers against the *camera*, never against the object it falls on (`object.layers.test(camera.layers)` is the only test in the renderer), so a light cannot be aimed at one character. Instead the player's materials are cloned and given an emissive term driven by their own texture, which brightens the artwork already there, scales with the board, and cannot reach anything else. Measured against the backdrop behind him: 18.6 vs 27.4 before, 29.2 vs 27.4 after.
- 2026-08-28 — **Cover mechanic.** Shooters now fight from behind the boards' boxes: `findCover` in `enemy.ts` scans collision boxes near the enemy for a hide point on the far side of a box that (a) blocks the sightline in both directions — the eye-to-chest ray the player sees *and* the chest-to-target ray `updateVolley` fires along, so hidden always means holding fire — (b) sits within 0.9× blaster range of the target, (c) has real ground under it and a walkable straight path (sampled every 2 m, so nobody crosses a platform gap to reach a crate), and (d) has a peek point off the box's edge where the muzzle ray opens. The cover loop then cycles hide (0.6–1.8 s, volley cancelled — ducking holds fire) → peek (pop out with attack cooldown clamped, fire one volley) → duck back, with a 20% chance per cycle to relocate; committed shooters run the same loop at ~2.2× tempo so the director's pressure roles survive, suppression stretches hiding ~3×, being shot in the open triggers an immediate cover search, and a flanked spot (sightline reopened) is abandoned. Two bugs found by the sims along the way: hide/peek validation originally used different ray endpoints than the firing check, letting marginal cover (the 1.5 m barrels) leak volleys from "hidden" — fixed by validating with the muzzle ray itself plus the explicit hold-fire rule; and the new station edge guard (probe the ground 1.2 m ahead of any walking enemy's velocity, stop at the lip) initially gated on speed > 0.5 m/s, which let blocked enemies *creep* off the platform at 0.3 m/s as steering re-added velocity each frame — 6/8 then 4/8 walkers fell chasing a cross-platform noise, 0/8 after gating near zero. Knockbacks still throw enemies off platforms (deliberate). Verified: 3/3 station shooters use crates with volleys only while peeking (180 peek frames, 0 hide frames), desert shooters cycle behind the homestead hut, 41 engaged hostiles with cover AI cost 0.30 ms/update, browser boot clean on both boards.
- 2026-08-28 — **Aim and melee arm fixes, and a workbench that keeps its edits.** Two defects reported from the workbench, both in the off arm. (1) The **melee guard pose** put the free hand *behind the back*: measured in chest space, `handL` sat at z −0.10 to −0.25 through melee1 and melee2, a leftover from the arm-splay sign fix, which had spared the two-handed grips on the theory that the off hand comes across to the weapon. It does not — it hangs free. The corrected guard from the workbench export is now applied to every clip that shared it (melee1, melee2, and the recovery keys of melee3 and enemySwing, whose overhead two-hand grip keys are untouched); every key of every melee clip now holds the hand in front of the body. (2) The **aim poses** held both arms angled *into* the ribs (`upperArmR` z −4°, `upperArmL` z +10°) at near-full extension — the "too tight" read. Both aim clips (`aimUpper`, played by standing and running aim, and `enemyAimUpper` for the ranged enemies) gained shoulder-girdle tracks that lift and open the shoulders, arms opened outward, and a properly bent firing elbow; the elbows now clear the torso by 7–8 cm more. The wrist takes a compensating rotation computed so the weapon's world orientation is unchanged — measured at 0.06° of barrel deflection — because the muzzle is where shots and tracers originate. Verified by measuring hand, elbow and muzzle positions in chest space for every key of every clip touched, then by eye in the workbench. **Workbench**: pose edits now live in a ledger (`src/workbench/poseEdits.ts`) that holds a delta per clip and bone and writes it into the live `AnimationClip`s, so an edit outlives edit mode — leave it and the animation plays back adjusted — and survives a change of pose or character (a newly built character has its pristine clips recorded, then the session's edits re-applied). Undo/redo (buttons and Ctrl/Cmd+Z, Ctrl/Cmd+Shift+Z) walk that ledger, and **Export changes** now emits every edit from the whole session in one file, grouped by clip, with the original keys and the finished ones ready to paste into `clips.ts`.
- 2026-08-28 — **Pose editing in the model workbench.** An *Edit mode* toggle freezes the pose at its first keyframe (the mixer stops advancing, so the bones stay where the clip put them), draws the rig over the figure as clickable joints, and hangs a rotation gizmo off the joint you pick. The gizmo's rings can be oriented three ways — the bone's **local** axes (the space the clip keyframes are written in), **world**, or **camera**, where the rings are screen-right, screen-up and roll-in-the-screen-plane, which is the one you want for "swing that elbow a bit further out from where I'm standing"; in local and world space an extra outer ring rolls about the view direction, so camera-relative roll is always available. Shift snaps to 5°, the three number fields take exact degrees, and rotations apply to every figure on the turntable at once so the authored skin and the procedural build stay in the same pose. **Export changes** downloads a JSON of the edited bones — absolute local euler XYZ in degrees (the argument order of `qt()` in `clips.ts`), the delta from the clip's value, and the keyframes that bone holds today — which is enough to update a clip by hand, whether it holds one value across the clip or moves through several keys. Rings preempt the orbit camera on hover; joints are picked on a click that did not become an orbit drag, so the camera stays free. Nothing is written to the clips: leaving edit mode restores the pose and hands the animator back.
- 2026-08-28 — **Player snap-to-cover** (RDR2-style). The slam button splits by grounded state: in the air it is still the ground slam, on the ground near a box (≥1 m tall/wide, within 2.4 m — a HUD prompt appears) it snaps the player against the nearest face. Tucked: slide along the face with the stick (clamped to the box), protected by real geometry — enemy sightlines and bolts stop at the crate — and holding fire. Hold aim to lean ~1 m out past a corner and shoot: corner picked by camera lean but overridden by a raycast test for a clear shot to the soft-locked target, re-checked ~3×/s (crates sit in rows; leaning into the neighbouring crate is a peek wasted — this and the lean depth both came out of headless probes against the station's double crate stack). Release aim to duck back; jump/dash/melee/cover-again/pushing off the wall all exit. Weapon switch disabled in cover, rockets only while leaned out; Dead Eye combines freely. Two bugs caught in testing: the toggle press that entered cover was also read as the exit press in the same frame (entry now masks it), and the peek-side chooser originally cast its lock cone from the camera, which is a frame stale on the first aim frame (now cast from the chest). Verified headless end-to-end: prompt in range, snap to face+0.67 m, 6 s under enemy fire tucked = 0 damage with enemy LOS blocked, lean-out fires and drops the target 85→17 hp, released = tucked and silent with fire held, jump exits upward, re-enter and slide clamped to the face, push-away exits; in-browser: walk to crate → prompt → C → "IN COVER" state, no console errors.
- 2026-08-27 — **Controls and settings pages.** Two icon buttons join fullscreen in the bottom-right corner: **(i)** opens a controls reference — an inline-SVG Xbox pad with every binding called out, transcribed from `core/input.ts`, alongside the keyboard column — and a **gear** opens audio settings with master/SFX/music sliders that apply live and persist per device. Both are reachable from the title and pause menus too, and remember where they were opened from rather than dumping you on the title. The title screen's wall of instruction text is gone, since the controls now have a page of their own. `MenuScreen` gained slider rows, where left/right adjust the value instead of moving the focus — the only way a stick or d-pad can change one. Two real menu defects surfaced while testing this and are fixed: a clicked `<button>` kept DOM focus so the browser re-fired it on the next Enter, hijacking keyboard and controller selection; and a screen opening under a stationary cursor fired `mouseenter`, dragging focus onto whatever sat under the pointer — usually the button just clicked — so hover now only counts once the pointer has actually moved.
- 2026-08-28 — **Ragdoll deaths and persistent corpses.** Killing blows no longer play the canned death clip: the mixer simply stops being updated (freezing the last live pose), the body tips over pivoting at the feet along the accumulated kill impulse — sampled on the first dead frame so knockbacks stacked after the damage call (explosions, melee) still steer the fall — with felled-tree acceleration, a thud, and a small bounce off hard falls, while ten limb bones flail with damped random angular velocities scaled to the hit and the capsule flies and slides to rest. Enemies already prone (wounded crawlers, knockdowns) keep their held pose rather than double-tipping through the floor. Corpses now stay where they fell for the whole wave instead of sinking away after 2.6 s — settled bodies early-return from update (40 corpses lying: 0.04 ms/update) — and fade out over 1.2 s when the wave clears; materials are cloned per corpse at fade start because the procedural builder shares them from a cache, and corpses below the kill plane (knocked off the station) are removed. Verified headless: tip 0→86° in ~0.4 s, 3.6 m slide along the impulse, corpse present 20 s later, faded and removed 1.3 s after wave clear, a nikto shot down at 26 m settles on the sand, a bled-out crawler persists prone without ragdolling; staged three kills in-browser and photographed the bodies sprawled where they fell, no console errors.
- 2026-08-27 — **Control overhaul and the block shield.** Switch weapon moved to **D-pad right**, freeing the shoulder buttons; **RB** became the movement button and **B** the shield; cover/ground slam took RB's old place on **LB**. RB reads its meaning from the stick, which is the interesting part: pressed with the stick centred it *arms* a dash and waits, and the first direction pushed spends it; pressed while already moving it is a sprint for as long as it is held; in the air, where sprint means nothing, it is always the jet burst. Holding on through a dash rolls into a sprint when it lands. **Block** (hold B / R) raises a curved force pane in front of the chest. Enemy bolts arriving on its outward face are reflected about the shield normal, keep flying as the blocker's own fire at 75% damage, and spark where they bounce; the pane only collides once it is most of the way up. It drains the sprint gauge (5 s from full), caps movement at 3.2 m/s, stows the weapon and gates firing, melee and weapon swap, and — raised in the air — cuts the jetpack and pulls you down, because a brace needs the ground under it. Verified numerically: RB while moving takes 9.2 → 14.4 m/s with no dash fired; RB at a standstill fires nothing until a direction is pushed, then dashes at 19 m/s; blocking holds 3.2 m/s, drains 0.2/s, refuses sprint and dash, falls 9.2 m in 0.75 s with the jetpack held, and takes 0 damage from a bolt that deflects.
- 2026-08-27 — **Six more authored models integrated.** Bo-Katan Kryze and The Armorer join the playable roster (four Mandalorians now, all with authored skins). The assassin droid, death trooper and dark trooper pick up their models on the enemy side; the line stormtrooper stays procedural, since only the elite has a skin. The Cad Bane-class **duelist** had no home at all — `ASSETS_MODELS.md` lists it as a planned boss — so rather than leave the model unused it enters as a late-wave elite gunslinger: fast (7.2 m/s), accurate, hits for 16, and folds if you can close on him. He appears from wave 7 in the Dune Sea and wave 8 on the station. The swap itself moved into `attachAuthored()` in `characters/authored.ts`, so a character is wired up in one call and the enemy side gets the same hide-the-procedural-body-and-retarget path the Mandalorians already had; the enemy weapon stays on the canonical `weaponR` bone rather than moving into the model's hand, because the firing code looks for the muzzle there. Every factory with a model takes an `authored` flag so the workbench can stand the two builds side by side, and all fourteen characters are now in the workbench picker. Verified in a real match at wave 9: every droid, death trooper, dark trooper and the duelist carries its authored skin, characters without one stay procedural, and no console errors.
- 2026-08-27 — **Six more models in.** The two pirates, Cobb Vanth, Fennec Shand and the Nikto swoop rider all take their authored skins; the Moff-class **Imperial officer** was the second planned boss with no home in the game, so like the duelist he enters as a late-wave elite — a darksaber melee bruiser (240 hp, 26 damage, 3 m reach) to answer the duelist's rifle, from wave 9 in the Dune Sea and wave 10 on the station. His blade is an FX mesh: an opaque, depth-writing black core with the glow sitting a hair behind it, so only the rim of the halo shows past the edges — otherwise additive blending washes the core white and it reads as a plain sword. The **Nikto** was the interesting one: the rider is a whole biped on the canonical rig, just held in one pose rather than animated, so the swap works exactly as it does for anyone else and the retarget simply reproduces the seated pose each frame; only the saddle offset needed changing, since it had been tuned to the procedural rider's proportions. Two id mismatches cost a round of debugging and are now documented: the officer's file is `imperial_officer.glb` against the kind `officer`, and the melee pirate's is `pirate_melee.glb` against the kind `pirateMelee` — the first silently fell back to the procedural build until the filename was fixed, and the workbench note now reads the real filename. Verified at wave 10: every pirate, Nikto, droid, death trooper, dark trooper, the duelist, the officer and both authored allies carry their skins; IG-11, Tuskens, Pykes and line stormtroopers have no model yet and stay procedural.
- 2026-08-28 — **War massiff brought back, promoted.** The massiff was the only enemy ever cut for being small (a 0.8 m quadruped, retired so every enemy would be human-size or larger); it returns rebuilt as an elite rather than deleted. New procedural build in `characters/enemies.ts`: 1.37 m wide x 2.12 m tall x 5.58 m long against a trooper's 0.78 x 1.94 x 0.4 — slab-sided flattened torso under a spine of dorsal plates and spikes, flank scutes, shoulder hump, heavy tusked skull slung low and forward on a thick neck, five-segment spiked tail, four heavy clawed legs on a diagonal-pair stalking gait. Stats put it in the elite tier: 300 HP, 30 damage, 3.6 m reach, spawning on the desert board from wave 5 (1) to wave 10 (3) instead of wave 1. Two behaviour additions back it up: a `relentless` Def flag (exempt from the director's standoff rotation and from morale breaks — a predator neither waits its turn nor runs), and a ballistic **pounce** that closes 4-16 m, led to intercept the target's velocity so it lands on a runner rather than behind them, with no air steering so a dash or jetpack hop beats it. Measured balance: jogging away cannot escape it (closes to 0.0-0.3 m and stays within 4 m), sprinting away does (opens to ~19 m) but burns ~70% of the energy gauge in 5 s. Also added `hitParts` — extra hit spheres on the skull and haunches, since one 0.85 m capsule sphere left shots at a five-metre body passing through the head (verified: skull, torso and haunch bolts all register). Its two retired sounds are re-requested in `ASSETS_AUDIO.md`; until they land `audio.beastGrowl/beastYelp` synthesize a snarl and a death cry, since the bark table has no fallback and a silent charging beast reads as broken. Added to the model workbench, and `ASSETS_MODELS.md` flags the size change so authored art matches the new scale rather than the old sheets. Verified integration with the newer systems: posted and idle at its spawn like everyone else, ragdolls correctly as a rigless character, 45 engaged hostiles including three massiffs cost 0.74 ms/update.
- 2026-08-28 — **Dead Eye removed; the right stick now sets camera distance.** The slow-motion mechanic is gone entirely — timescale plumbing in `Game.update` (the whole world ran on a scaled clock while players ran on the wall clock), the meter and its HUD bar, the sepia canvas tint, the damage bonus and spread exemption, the kill refill, and the `V` / R3 binding. In its place, holding the right stick in makes the stick's vertical axis dolly the chase camera (yaw still works, pitch is suspended while held); the mouse wheel does the same for keyboard and mouse. The distance the player lands on is **sticky** — `ThirdPersonCamera.baseDist` persists as the new default until it is changed again, clamped to 1.9-11 m — and aiming now pulls in proportionally (59% of the chosen distance) rather than to a fixed 2.7 m, so the over-the-shoulder framing keeps its relationship at any zoom. Verified: 4.6 m default, ~3.3 m of travel per second at full stick, sticky across release, clamps at both ends, and at an 8 m base the aim pull goes 8.0 -> 4.7 -> 8.0 m. Player fire rate and cooldowns went back on the normal clock now that there is no world/player clock split.
- 2026-08-27 — **The rest of the models.** Tusken raiders, Pyke soldiers and line stormtroopers take their skins, so every character in the game now has one except IG-11. The last two planned bosses arrived too: the **Pyke capo** (shielded shooter, 260 hp, personal shield bubble) and the **Krrsantan-class Wookiee enforcer** (2.6 m, 420 hp, 34 damage) — and unlike the duelist and the officer, these two spawn *only on the final wave*, one each, which reads as a boss rather than another body in the crowd. A second intake path went in for models nothing on our rig drives: `loadProp()` handles the **swoop bike**, which now replaces the procedural box the Nikto rode, and the **carbine and gaffi**, which swap under the procedural weapons so every mount, muzzle and swing clip is untouched (the gaffi needed a quarter turn — the sculpts lie along their longest axis, Z, while the procedural staff was built along Y). And a third for creatures: the **massiff** is a quadruped on a 44-bone rig with four legs and a tail, so no humanoid clip has anything to say to it; `loadCreature()` places, scales and grounds it and leaves the movement to the enemy carrying it. That is the intake ready for whoever adds the massiff back as an enemy — the model side is done and documented in `ASSETS_MODELS.md`; the gameplay side is not this change. Verified in a wave-10 match: 12/12 Tuskens, 7/7 Pykes, 6/6 stormtroopers and every other authored kind carrying its skin, IG-11 alone still procedural.
- 2026-08-28 — Wired the war massiff to the authored assets that landed alongside it, and gave it its voice. The model side arrived from the other track as `massiff.glb` plus a `loadCreature()` intake path explicitly left "ready for whoever adds the massiff back as an enemy" — that is this enemy, so it now loads through it, with the procedural build kept as the fallback and hidden the moment the sculpt lands. Two things had to be fixed for the rigged quadruped that is still being authored: `loadRaw` was discarding `gltf.animations`, so a creature carrying its own clips would have arrived mute, and `loadProp` cloned skinned meshes without rebinding them to the clone's bones, so anything animating the clone would have moved the original's skeleton instead (the biped path already rebinds; the prop path never needed to until a creature came through it). `buildMassiff` now runs a mixer when the file ships clips, picking idle and run by name match, cross-fading between them by speed and rate-matching the run so the feet don't skate; a new `CharacterInstance.setGait(speed)` feeds it the beast's real ground speed from `Enemy.syncVisual`. The creature's registered height was also raised from 1.15 m to 2.05 m — the sculpt was scaled for the old knee-high hound, not the elite. Finally, the two retired sounds were regenerated with ElevenLabs at the new size ("huge armoured reptilian war beast", chest-deep growl and a death roar into a gurgling rattle rather than a dog's bark), committed as `massiff_growl.mp3` / `massiff_yelp.mp3`, with their prompts added to `tools/generate-sfx.mjs`; verified decoding in-browser at 1.2 s, full-scale peak. `audio.beastGrowl/beastYelp` already preferred the samples over their synth fallback, so they are live with no further wiring.
- 2026-08-28 — **Browser test harness, and keyboard/mouse restored as the secondary path.** `tools/harness.mjs` drives the real build in Chromium behind a synthetic Xbox pad: an init script replaces `navigator.getGamepads()` with a fake standard-mapping device whose buttons and axes are driven from the test, which the input layer cannot tell from real hardware, so menus and gameplay are exercised for real rather than mocked. It exposes `pad.tap/down/up/stick/trigger`, a `startMatch()` that walks title → board → character select, and a `step(seconds, input)` that advances the game loop directly — necessary because software rendering runs at a few frames a second, so waiting in wall-clock time for anything in-game is hopeless. Two things had to be handled: the synthetic `gamepadconnected` event is impossible to construct (its init dictionary demands a real `Gamepad`) and unnecessary, since the game polls every frame; and character select refuses to lock a pick in until that character's `.glb` has loaded, so presses are retried rather than fired once. Gameplay keyboard and mouse — removed when input went controller-only — are back as a deliberately secondary path (WASD, mouse look, Shift sprint, LMB/RMB fire and aim, F/Q/E/C/R, wheel to dolly the camera). Menus took clicks already except the new 3D character select, which was pad-only: a press that travels less than 6 px now counts as a click on that pedestal and runs the same three beats as A (join → lock in → start), so drag-to-turn still works. Verified with the harness: the full title-to-match flow with the mouse alone, every keyboard and mouse action confirmed by its in-game effect (E switches weapon, Q fires the rocket at cooldown 11.9, F swings, the wheel moves the camera 4.6 → 9.02 m), and the pad path unchanged.
- 2026-08-27 — **The massiff has a gait.** Its model ships no animation — none of the 24 models do — so the mixer the enemy already had was dead code with nothing to play. `src/anim/quadruped.ts` now authors an idle and a gallop against the massiff's own quadruped skeleton, and the loader falls back to them only when a file carries no clips of its own, so a later re-export with baked animation takes over with no code change. Two things had to be right. This skeleton has **real rest rotations** — a thigh already points down and out — where our biped clips assume an all-identity rest, so every value is `rest * delta` and the clips are built at load time once the rest pose can be read. And a leg has to **hold its rest height through the stance half** of the cycle: the first version kept a constant bend in the shin, and measuring the foot showed it sitting 0.27 m above its rest height for most of the stride — the animal prancing above the ground it was supposed to be pushing off, because nothing raises the body. Split into stance and swing, the foot now plants at exactly its rest height and lifts 0.23 m to clear. Verified in a real match: the massiff spawns with its skin, and once it wakes it charges at 20 m/s with the gallop rate-matched to that speed. The workbench's run poses drive `setGait`, so the gait can be reviewed there.
- 2026-08-28 — **Controller-first input, with keyboard and mouse behind a setting.** A new `config.input.keyboardMouse` (off by default, saved per device under `mando.input`) gates the whole keyboard-and-mouse gameplay path: while it is off `read()` never consults the keyboard or mouse, so no stray key or click moves the player. In-game pointer lock is gone with it — `requestPointerLock` now returns early unless the setting is on, since the lock only ever existed for mouse look — so the cursor behaves normally over the page during a controller game. It also follows console convention rather than simply always showing: hidden while playing, back on the first mouse move so the corner buttons stay reachable, hidden again after two idle seconds (`updateCursor` in `main.ts`, a `cursor-hidden` class on the body). The Controls screen no longer documents bindings that do nothing — `controlsMarkup(keyboard)` swaps the keyboard column for a note pointing at the setting, and the always-available menu keys moved into their own list so they are shown either way. Menus stay mouse- and keyboard-navigable at all times, since a menu is not gameplay. Added `MenuScreen.addToggle` for the on/off row (confirm flips it, left/right set it, repainted with the sliders when a screen opens) and a `clickText` helper to the test harness: Playwright's locator click waits for an element to be "stable" across animation frames, which never happens when software rendering starves the frame loop and a paused game is still drawing behind the menu, so menu clicks go through raw coordinates instead — the elements themselves were never obstructed. Verified both modes with the harness: off — W does nothing, no pointer lock, cursor hides and wakes on movement, Controls hides WASD and points at Settings; on (enabled entirely by mouse) — Controls lists WASD, W drives the player, pointer locks for look.
- 2026-08-28 — **Six new boards, and the board systems they ride on.** The `Board` interface stopped being a two-way switch: a board now carries its own identity (name, banner, footstep surface, ambience sample + synth bed, music playlist, per-board wave table in `spawner.ts`) plus optional shared systems any board can declare — multi-zone **hazards** (`kill` circles like the sarlacc, `burn` zones and a free-form `burnAt` field for lava rivers and open water, damage landed in ticks so the feedback reads as heat rather than a buzzer), **movers** (kinematic platforms that re-place their physics box each frame and carry whoever stands on them), **breakables** (props with HP registered as team-2 bolt targets, damaged by bolts, explosions and ground slams, losing their collision box on death, optionally exploding — chains), **tractionAt** (scales only voluntary ground steering, so knockbacks still carry), and **lightAt** (a 0–1 light field the AI's sight check reads: enemies spot a target in the dark at roughly half range). The boards: **Nevarro** (basalt flats, two winding lava rivers as burn zones, breakable crust-plate bridges, geysers on a telegraphed cycle that damage and launch enemies but boost a player who rides them, adobe town gate); **the Crevasse** (Maldo Kreis — snowfield rims over a canyon floor, ice ledges and arches as a middle layer, traction 0.2–0.85 by surface, a frozen lake of nine breakable plates over bleed-out water that a ground slam opens, krykna nests, glacier groans); **Trask** (dock fingers over a cold harbour that bites at 20 dps, two trawler movers heaving and surging on a real swell — the sea mesh rolls with them — rain, delayed thunder after each lightning flash, and the mamacore kill pool between the piers); **the Refinery** (first interior: walled work halls under a 7 m ceiling around an open 40 m reactor shaft ringed with catwalks, 13 chaining rhydonium barrels, and four alarm consoles that call the whole garrison every 16 s while any squad is engaged — shoot them out to keep fights local); **the Great Forge** (Mandalore — fused glass terrain with a smooth low-roughness surface, the shattered dome with surviving roof slabs, floating ruin chunks, a 65-second storm cycle whose arcs strike anything without a roof overhead — checked by an upward raycast, so the AI's cover and the dome both count — and the Living Waters, where a mythosaur eye glows and a sub-bass call plays on a long random timer); **the Ringworld** (Glavis street strip, gravity 0.85, stepped walkable rooftops with neon signage, a ping-pong tram mover the length of the board, and a terminator sweeping on a 210-second cycle: the sun tracks the day side, a soft-edged darkness plane tracks the line, and `lightAt` makes the night side genuinely safer to cross). Seven new enemy kinds: **incinerator trooper** (flame stream committed to an aim line at trigger-squeeze — sidestep beats it), **krykna** and **broodmother** (free-form eight-leg skitterer rigs, relentless like the massiff; the brood spawns hatchling reinforcements each 22% of HP lost, wired through a new `Game.addReinforcement` so the wave count stays honest), **Quarren netcaster** (net bolt via a new projectile tag: 2.4 s leg snare the player walks off slowly or cuts with a melee swing; aquatic — immune to the harbour), **alamite** charger, **interceptor drone** (probe-style kamikaze: stalks, whines, and commits to an unsteered dive — requires 3 m of altitude so it can't fuse itself on the ground), and the **ringworld enforcer**, whose frontal shield collider reflects bolts (the deflection system the player's block uses, now on an enemy) — flank, melee or rocket. Board select is a registry-driven 4×2 card grid; the audio engine gained snow/stone footsteps, six ambience hooks and seven synth voices (thunder, geyser, klaxon, ice crack, flame, mythosaur). All verified headless: `tsc` + `vite build` clean, all 8 boards boot through the real menus and run 10 s of combat with zero page errors, and a force-spawn test ran each new enemy kind against the player (flame stream kills a standing target, krykna/alamite close and hit, netcaster snares at 5 damage, brood closes, drone dives and detonates, enforcer deflects while advancing). Fresh asset requests opened across all three docs.
- 2026-08-28 — **Water became a place instead of a wall.** `Board.waterY` declares a sea surface and the engine does the rest. The **player** wades where the bottom is within standing depth (speed ×0.45, no sprint/dash, footfalls become splashes) and **swims** where it isn't: camera-directed 3D movement like the jetpack (Space rises, a level camera holds an eye-at-the-surface equilibrium via gentle buoyancy), the dash button is a fin-kick off the energy gauge, a jump taken near the surface breaches 8.5 m/s up into a jetpack chain, and the sealed helmet means no air limit — depth costs reach, not health. **Bolts die at the surface in both directions** (a splash where they hit), so a diver travels and hides but has to surface to fight; guns are holstered underwater and the gaffi still swings. **Hostiles all but lose sight of a submerged target** (notice capped at 7 m), which makes the water the stealth route onto any defended platform; a splash makes noise, so a clumsy entry still gives you away. Non-aquatic **enemies drown** if their heads stay under (Quarren, flagged aquatic, are at home), and the walking AI's edge guard now also refuses to steer over a lip into deep water while still allowing Trask's chest-deep harbour crossings. Rendering is per-viewport: each split-screen camera judges the surface for itself and gets dense teal murk and a matching background below it, so a diver and a surface partner see different worlds. **Trask reworked on playtest feedback** — the flat 20 dps water is gone. The harbour is wadeable and safe on its own; the danger is the **mamacore on a clock**: ~5 s of cumulative water time and a ripple wake (visible torus + spray + a low roar) converges on the swimmer over 2.4 s — exactly the window to reach a ladder or jet clear — then strikes for 55 with a fling, and circles back fast. Its lair pool stays instantly lethal. And the ninth board: **"The Prison Rig"** (`narkina`), a sterile white Imperial facility on pylons over a deep ocean, Narkina-flavoured. Above: a deck ring around an open moon pool, four bridged satellite pads, and **electrified floor sections** on a 16-second cycle — 2.2 s of blinking charge with a rising capacitor whine, then 2.8 s live, shocking players *and* guards standing on the plate (suppression included), so every firefight has a rhythm of hops and repositioning. Below: a full seafloor to explore — kelp forest swaying over a trench, a glowing teal reef with a circling fish school, a sunken transport hull you can swim through, pylons all the way down — and the moon pool surfaces inside the perimeter behind every posted sentry. Verified with a new harness suite: swim engages in deep water at zero health cost with 14.7 m of headway in 3 s; a stormtrooper staring straight at a diver 15 m away never engages, then spots them within seconds of surfacing; Trask wading holds 100 HP where the old water would have taken ~60, and the mamacore takes a lingerer to 45 and launches them; all 9 boards boot and run combat with zero page errors. Asset requests added (card, sky, sea ambience, splash/roar/floor-charge voices).
- 2026-08-28 — Integrated the uploaded art batch. **IG-11's model** was the only character still running procedural-only: `buildIG` now takes the `authored` flag and calls `authoredEnemy(..., 'ig11')` like every other character, with `ig11: 2.2` added to `AUTHORED_ENEMY` to match its DEFS height (the procedural build measures 2.22 m, so the sculpt normalises onto the same silhouette) and to `ENEMY_MODEL_ID` so the preloader knows about it. That last part turned out to matter: the model loaded in the workbench but never in a match, and tracing the loader showed GLTFLoader's callback simply never firing for `ig11.glb` — no error, no warning, a clean 200 on the wire, while 137 other loads resolved in the same session. The file is sound (valid GLB chunks, the same extensions and a complete Rigify skeleton as `tusken.glb`); what was wrong is *when* it loaded. Allies are not part of a wave's composition, so `preloadWave` never warmed them — the marshal, IG-11 and Fennec each began a cold 3.9 MB download and decode at the exact moment they walked into a firefight, against a wave spawning thirty enemies. Allies now share one `ALLY_WAVES` table between the spawner and the preloader, and all three are warmed at match start, which both fixes the stall and removes a mid-fight hitch; verified in a live match, IG-11's ally now carries one skinned mesh with 23 procedural meshes hidden, the same shape as a working tusken swap. **The 15 board textures** needed no code — every one (`board_nevarro/crevasse/trask/refinery/forge/ringworld`, `sky_ice/mandalore/nevarro/ring/trask`, `basalt_albedo`, `ice_albedo`, `lava_flow`, `snow_albedo`) was already requested by name by the board modules, so arriving at the documented path *is* the integration; confirmed live by booting The Lava Flats and finding `scene.background` had become a 4096×2048 texture rather than the procedural dome. **Docs bookkeeping:** 43 delivered image requests moved from `ASSETS_IMAGES.md` to `ASSETS_COMPLETED.md` with their prompts, leaving only The Prison Rig's two files (`board_narkina`, `sky_narkina`, both with working fallbacks) genuinely open; the character-sheet generation recipe stayed behind in the requests doc since future characters will need it. `ASSETS_MODELS.md` no longer lists `ig11` as outstanding — what remains is the seven new-board enemies, all of which now have their reference sheets, so they are ready to model.
- 2026-08-28 — **Co-op goes to four.** Split-screen geometry moved into `core/layout.ts`, so the renderer's viewports and the HUD's DOM panels are cut from the same rectangles and cannot drift apart: one player full-screen, two top-and-bottom as before, three as two quarters above a full-width strip — a solo bottom strip reads as a layout rather than a missing fourth — and four a quadrant each. Input carries four pad slots, the main loop reads all of them, and the character select has four pedestals. Two things were quietly capping it at two beyond the obvious: `padForPlayer` was a two-element array, and `slotFor` in the character select mapped only pads 0 and 1, so a third controller's A press resolved to no slot at all and simply did nothing. The character-select panels are now positioned by projecting each pedestal to the screen rather than sitting in equal flex columns — the old arrangement lined up with two only because the spacing had been chosen to make it, and only at one aspect ratio. Boards still declare two start points; a third and fourth are fanned out sideways from them, keeping the height of the spot they extend, rather than asking nine board files each to remember a number. Wave sizes scale with the party (half a wave's worth per extra body) instead of the old two-player special case, and the HUD shrinks its furniture in a quarter-sized viewport. `window.__startCoop(n)` starts an n-player match without n controllers, since split-screen is otherwise untestable on one machine. Fixed alongside it: clicking a corner button (controls, settings, fullscreen) left DOM focus on that button, so the menu's Enter handler saw the press land there instead of on the focused menu item and the keyboard went dead until you clicked elsewhere — the buttons now blur themselves on click, as the menu buttons already did.
- 2026-08-28 — **The Prison Rig's art and three of the seven new-board enemies arrived.** `board_narkina.jpg` and `sky_narkina.jpg` were already referenced by `boards.ts` and `narkina.ts`, so both were picked up with no code change — every one of the nine boards now has real card art, and the last two open image requests are closed. The models for the **incinerator trooper**, the **Quarren netcaster** and the **alamite** landed against builders and `AUTHORED_ENEMY` entries that were already written for them, so they had been falling back to their procedural builds; all three now load (33 of 33 bones mapped each) and retarget cleanly, verified in a real match rather than in isolation — the quarren skinned on all four spawns on Trask, the alamite on all six on the Great Forge, the incinerator trooper on Nevarro at wave 4, with a clean console. Worth noting for the next intake: the skin arrives on a network round trip, so a probe that samples once after stepping the sim sees `skinned: 0` and reads as a failure; wait for the SkinnedMesh instead. The workbench picker was missing the whole new-board roster, so all seven kinds went in — the three with models get the model/procedural compare view, and `ring_enforcer`, the krykna, the broodmother and the interceptor drone stand as procedural-only until their `.glb`s arrive.
- 2026-08-28 — **A collision audit for every board, and the holes it found.** Reported from play: the raised stone drums around Nevarro's geysers were scenery you walked straight through. Rather than fix the one prop, `tools/audit-collision.mjs` now measures the question across all nine boards — it builds each one in a real browser (through a new `__boards` debug handle), walks every mesh, and tests its footprint against that board's boxes, cylinders and heightfield. It reports both directions: geometry that looks solid with no collider under it, and colliders with no geometry on them (invisible walls). Decoration is excluded by rule — point clouds, additive glow panes, sub-30 cm decals and rigging, the world-sized meshes — plus an explicit `userData.decor` flag a board author sets to say "this is meant to be swum through", now on the kelp, the coral, and the writhing tentacles at both sarlacc and mamacore. Getting the tool honest mattered as much as the fixes: probing a mesh's world AABB flagged every *rotated* slab (the Forge's dome walls, the sunken transport) because the probes landed in the empty corners of the box, and probing a cone's box called its taper a hole. It now samples each object's own footprint, using the geometry's real radius where it has one. **Fixed:** Nevarro's five geyser drums (the reported bug — solid now, a 0.85 m lip you hop onto, and riding the blast means being on the pad when it fires); the Dune Sea's homestead dome (one inscribed disc left the skirt walk-through — two stacked discs follow the curve), the sail barge's 9 m leaning sail, and the ring of sarlacc fangs, which stands outside the kill radius and is the first thing anyone charging the pit meets; the waystation's three cargo cranes, which were pure scenery — 18 m mast, 20 m arm and a hanging container, in the middle of the airspace the board is fought in — plus the machinery slung under every platform, which the jetpack puts you against constantly, and the freighter's cockpit blister; Trask's trawler masts, now riding the same swell as the decks they stand on; and the Prison Rig's sunken transport, whose three hand-placed boxes left the yawed hull open at the ends while parking invisible steel in the water beside it. Colliders on sloping ground now reach below their own meshes, so nothing can be walked under from the downhill side. All nine boards come back clean in both directions, and the audit is wired into the deploy workflow so a board that loses a collider fails CI rather than shipping. Verified beyond the audit: props hold a dropped player and stop a walking one (the two that let a walker past were measured and are correct — a 33 cm step up onto a pad from a neighbouring crust plate, and an airborne crane beam you land on rather than walk into); the station carries 97 hostiles at 1.14 ms/update with the ~100 added colliders. One gameplay fix fell out of it: an idle diver used to drift up to visor-at-surface, which parked them exactly on the line the AI uses to decide whether it can see a submerged target, so hiding underwater worked or didn't at random. Beskar is neutrally buoyant now — let go and you hold your depth, surfacing is the jump button — and the stealth route is dependable.
- 2026-08-28 — **Split screen splits the way the screen wants it.** A *Split screen* setting picks between the stacked layouts and the same layouts turned on their side (two columns; three as two stacked beside one full-height player), for tall or portrait displays. It applies mid-match, and the HUD reads its rectangles from the same `splitLayout` the renderer does, so a panel can never drift out of its picture.
- 2026-08-28 — **Controllers earn their seat by being used.** A pad takes a player slot the first time somebody touches it — any button, or a stick past drift — rather than the moment the browser enumerates it, which is what once made a real second controller join as player three and drive nobody. Pads asleep in a drawer, unpaired dongles and one controller seen twice over two transports all prove nothing until used. Losing a pad mid-match pauses rather than shuffling players between characters.
- 2026-08-28 — **Assets warmed ahead, and a drop screen instead of a fade-in.** Every heavy file is pulled during the seconds spent in menus, in priority order, only while the browser reports itself idle and two at a time, so warming never starves the thing being waited on: the title warms the first fighter and the nine cards, the grid warms the rest of the roster, and choosing a territory warms that board's sky, ground textures and the models its opening waves will post. Starting a match then shows the territory's art, the fighters going in and the hostiles waiting there behind a byte-weighted progress bar, and holds until what would visibly pop has landed — a territory is never first seen as its procedural stand-in. A failed load settles like a finished one, so a missing file drops you in with the procedural version rather than stranding the bar; the wait is capped, and A drops you in regardless.
- 2026-08-29 — **The roster went to nine, and took original names.** The character factory now covers two families: helmeted Mandalorians and bare-headed underworld hunters (`helmet: null`), who bring their own procedural heads, exposed-skin colour and a signature weapon each. Joining Kell Dravan, Torva Brekk, Vess Ordane and The Forgemistress: **Sylla Morvane** (twin red curved-hilt sabers — additive FX blades, an off-hand hilt that shows and hides with the main one), **Karshii** (laser crossbow with a glowing string), **Skarvek** (long hunting rifle), **VX-9** (the ally droid, playable — no jetpack, so flight flames mount under the foot bones), and **Rook Vance** (twin heavy pistols, the same sculpt he fights as on the Ringworld's final wave). Paired weapons are now general rather than a saber special case, and two bugs fell out of it: a character who spawns holding a pair had one weapon invisible until the player happened to swap and swap back (the loadout settles at build time now), and an off-hand weapon stayed on our own `weaponL` bone while the authored model moved the main weapon into its hand — authored models expose a `weaponMountL` too, so the second weapon is in the other hand. Each signature weapon also got a voice, with synth fallbacks and generated samples (`crossbow_shot`, `longrifle_shot`, `saber_swing`, `saber_ignite`, `pistol_shot`).
- 2026-08-28 — **The last seven models integrated, and the creatures taught to move.** The three playable hunters and the ringworld enforcer needed no code — the loaders picked them up — but the three `loadCreature` creatures (krykna, broodmother, interceptor drone) froze solid the moment their models landed: the procedural gait switches off on load and the files ship no clips. They now animate through `GENERATED_CLIPS`, code-built idle/move cycles in `src/anim/quadruped.ts` against the models' own named nodes, blended and rate-matched to gait speed exactly like the massiff. One subtlety cost a debugging session: the `cycle()` helper wraps its phase, so using it for keyframe *times* puts t=0 after t=0.39 and the track dies silently — times are linear now. Also fixed: the broodmother's procedural egg sacs were parented to the root and floated over the authored model; the workbench's `applyPose` returned early on animator-less creatures before reaching its own `setGait` line, so run poses never drove any creature gait, the massiff's included.
- 2026-08-29 — **The deploy went green again, and the test stopped hardcoding the cast.** `test-loading` asserted on the literal name "Din Djarin", so the roster rename failed CI — and the run before it had been cancelled by the concurrency group, so nothing caught it. The test now derives what it expects from the game: a `__roster` debug handle exposes the playable ids and names, the warm check covers every fighter instead of a frozen list of four, and the drop-screen check compares against the name the character select actually displayed, so a rename or a new character can no longer break it. Two stale assumptions went with it: the "a missing model does not strand the drop" case relied on `krykna.glb` being absent from the repo (it ships now, so the request is blocked at the network layer instead, staging the failure on purpose), and the asset checks sampled "has it arrived?" once after a fixed sleep, which the bigger roster finally lost — they poll to a deadline now.
- 2026-08-29 — **Board music: openers, random rotation, one config file.** Four boards now open on a signature track — The Storm Docks on the sea shanty, The Crevasse on the ice theme, The Great Forge on the forge chant, The Prison Rig on its own — after which a board picks at random from the rest of its rotation instead of cycling in fixed order, never the same track twice running. Two more tracks play on any board. The whole map moved out of the engine into `src/core/music.ts`: the playlists, the any-board list and the per-board opener, so adding or re-pointing a track is a one-file edit and nothing else in the game names a track. Uploaded files are renamed to the directory's lowercase-hyphenated convention so their URLs need no escaping.
- 2026-08-29 — Documentation sweep: README, `PLAN.md` and the asset docs had drifted behind the code — two boards where there are nine, four playable characters where there are nine, the show names the roster no longer uses, a duplicated controls row with the shoulder buttons swapped against `input.ts` (dash/sprint is LB, cover/slam RB), Grogu still riding along after his removal, and models listed as outstanding that had already landed. The four signature-weapon props are recorded as **parked by decision** — the procedural versions stay — rather than sitting in the docs as an open request.
- 2026-08-29 — **The territory audit: environment models requested.** All nine board modules audited for where authored 3D would do the most: every structure, vehicle and hazard in the game is still procedural primitives (the trawlers players ride on Trask are three boxes, the sail barge is two) while every character wears an authored model, so the environments are the gap. `ASSETS_MODELS.md` gained a prioritized environment section — 20 props in three tiers (P1: the things ridden, shot or hidden behind every match — trawler, tram, sail barge, cargo crate, fuel/rhydonium barrel; P2: each board's landmark — freighter, cranes, reactor column, sunken transport, Nevarro gate, forge brazier, survey crawler, plus a new mythosaur-skull landmark for the Forge; P3: repeated dressing — vaporators, tents, alarm console, kiosk, shed, homestead) — each entry pinned to the collision envelope it must fill, since the audited colliders don't change with the model. Matching image-to-3D reference-sheet requests (shared preamble + per-prop prompts, `reference/props/`) and six runtime textures (Ringworld facade albedo+emissive, neon sign variants, rust hull, white facility panel, forge relief, kelp frond alpha card) opened in `ASSETS_IMAGES.md`, each with its one-line wiring note since none are pre-wired the way the character loaders were. Bookkeeping alongside: the uploaded 26 drop-screen portraits and 3 hunter weapon sheets moved to the history doc (portraits are live with no code change — the loading screen already probes `portrait_<id>.jpg`; the three hunter portraits stay open), and the weapon-prop models are now unblocked on the model side.
- 2026-08-29 — **Decided the three hunter weapon props stay procedural, and spent the effort on the blades instead.** The saber, crossbow and long rifle were the last open model requests; rendering all three showed the crossbow and long rifle already read at silhouette level from a chase camera 4.6 m back, and the saber's hilt is 15 cm of a 1-metre weapon half-hidden in a fist — everything the eye actually reads on it is FX geometry no `.glb` can supply. The swap path stays wired (`swapWeapon`), so any of the three can still be dropped in later; they are now recorded as a standing offer rather than an outstanding request. **What the sabers were missing was not geometry.** They had a three-layer additive blade, an ignition snap and a swing sound, but no light, no hum and no dedicated animation. Added: a **point light on the main blade**, parented under the blade group so it travels with the swing and costs nothing stowed — the renderer skips invisible subtrees, verified live as 0 lights stowed → 1 while drawn → 0 once the combo lapses (the off hand deliberately carries no light: two would double the cost for a glow the eye reads as one). A **looping hum** (`audio.setSaberHum`, built on the jetpack-voice pattern, with the same rebuild-once-the-sample-decodes rule) that sits at 0.55 while drawn and leans to 1.35 through a swing, cutting to nothing when stowed, blocking or dead, and torn down with the match alongside the jetpacks. A **crackling energy clash** on contact — `meleeHit` now takes the weapon kind, since an energy blade biting should not clang like the staff. And a **dual-wield combo** (`saber1/2/3`): the staff clips animate the right arm alone and park the left, which reads as a dead arm when there is a blade in it, so the new set alternates leads and finishes on a cross-slash that throws both blades apart. The `saber_hum` loop was generated with ElevenLabs (4 s, verified decoding in-browser) and the synth fallback covers its absence.
- 2026-08-29 — **Nobody spawns inside a wall any more.** Reported from the Refinery, and it was every board: the spawner checked a candidate spot with `solidAt`, a single point probe at chest height, while the mover treats a body as a capsule — so a spot half a metre from a hall wall passed the check and the body standing on it was inside the wall until the push-out threw it somewhere nobody chose. Behind that, `freeSpot` gave up after 40 samples and returned the known-bad point it started from, which on a corridor board is exactly where it ends up. `PhysicsWorld.capsuleFree` now asks what the mover asks — the same inflated box and cylinder test `stepCapsule` resolves against — and every spawn is judged with the capsule of the kind being spawned, so a massiff needs the metre of room a massiff needs. The search is denser and wider, and when nothing near the wanted spot fits it falls back to the squad's post and then to the board's own authored spawn points rather than to the bad spot. Air spawns are checked too (ring at altitude, then higher), as are the mid-wave brood hatchlings and the ally who walks in on waves 4, 7 and 9 — all three used to be placed with no check at all. `spawnWave` split into `planWave` (positions, no meshes) plus the build, so the new `tools/audit-spawns.mjs` can plan ~99,000 bodies across nine boards, ten waves and three player positions and measure every one: 7,688 were inside geometry before the fix (~6% of the Refinery's, ~16% of Nevarro's), none after. Wired into CI beside the collision audit. Verified live as well — a match on the Refinery, the Prison Rig and the Crevasse, no body overlapping a collider seven seconds in — and squads still spread as widely as before (4–6 clusters on wave 1, ~20 by wave 10, out to the board's full reach). Also fixed while in there: the test harness failed on the browser's own favicon 404, and counted the deliberate 404s of optional assets — a portrait or model that isn't there falls back by design — as page errors.
- 2026-08-29 — **The ambient life & backdrop pass, specified.** The audit's loose ideas are now a real spec — `PLAN.md` §16 — seven atmosphere features with their systems named: sky traffic on the Waystation and Ringworld (decor flyby paths, one close pass per ~90 s), a working landing pad on the Waystation (a freighter on the Mover system that lands, sits and lifts off on a cycle — real ground you can ride, never a crusher), banthas grazing at the Tusken camp (the massiff's `loadCreature` intake, mover-carried colliders, no team and no health — bolts stop on the hide and they amble off), a sandcrawler landmark on the Dune Sea's rim slope, procedural pipe runs and cycling steam vents in the Refinery, quay dressing on Trask (alpha-card nets, swell-riding buoys, fish racks that enter the cover system as thin solids), and two parallax silhouette-skyline layers beyond the Ringworld's bulkheads. Ground rules baked into the spec: atmosphere only, decor-flagged where intangible, collision-audited where solid, procedural-first with automatic upgrades when files land. Asset requests opened to match: four models (`bantha` ◆, `sandcrawler`, `pipe_rack`, `fish_rack` — the traffic and lander reuse the requested `freighter`), their reference sheets, three backdrop textures (`skyline_silhouette` ×2, `net_weave`), and four audio voices (`ship_pass`, `ship_landing`, `bantha_low`, `steam_hiss`; the gulls already live in `amb_rain`).
- 2026-08-29 — **Pilotable vehicles.** Rides with hit points parked around five boards (PLAN.md §17): swoops at the Dune Sea's Tusken camp and on a Ringworld plaza (the delivered `nikto_swoop.glb`, parked and stealable), scout speeder bikes at Nevarro's gate and the Forge's dome gap, a landspeeder at the homestead, cargo skiffs by the barge and moored in Trask's harbour channel. **RB (C on keyboard) is the contextual mount** — a ride in range wins the press over cover — and dismounts again; A hops off into a jetpack chain. Driving is the foot controller's camera-relative wish with real momentum (`damp` at λ 1.1–2.0 toward a 15–27 m/s top speed, so speed is carried, not had), a boost impulse on the dash button, and a hover spring over `max(ground, waterY)` — the skiff rides Trask's swell and skims the mamacore's bite depth. The vehicle moves through the same `moveCapsule` the characters use, so walls, steps and the heightfield all hold; losing more than 7 m/s to one costs hull HP at 2.2/(m/s). **Ramming is the weapon** (weapons stow while riding): contact above 6 m/s lands speed-scaled damage with a knockback and knockdown, tested along the hull's axis so a skiff hits with its bow, with a 0.5 s per-body memo so one pass hits once — and every body struck chips the hull 3. **The hull is the rider's HP**: while mounted, damage to the player redirects to the vehicle (kill zones excepted — the sarlacc doesn't care what you arrived on), vehicles register as team-2 bolt targets with spheres laid along their length, and explosions reach parked rides. At 0 HP the rider is thrown clear (ride momentum plus an up-and-out kick) and the wreck explodes for real through `Game.explode` — AoE, knockdowns, chained breakables — then is removed. Parked rides are solid (a yaw-fitted AABB added on park, removed on mount) so they block, shelter, and can be shot before you ever reach them. The rider sits real `rideLower/rideUpper` clips (saddle straddle, hands to the bars — a standing pose on the skiff's tiller platform) that retarget onto authored skins like every other clip; the engine hums per rider through `audio.setEngine`, jetpack-voice style, with `speeder_loop`/`speeder_ignite` requested and synth fallbacks live. Verified end to end in the real build (`tools/test-vehicles.mjs`, 17 checks): spawn on both test boards, prompt, mount, 21 m/s under throttle with the rider 0.15 m off the seat centre, a rammed Pyke 80→54 HP with the hull chipped 3, a shot-down swoop throwing its rider (vy 7.5, rider survives, wreck removed), rider damage landing 20/20 on the hull, RB dismount re-adding the parked collider, and the skiff sailing 30 m down the harbour channel at y 0.90 before fetching up against the moored trawler — plus the collision audit, co-op seating and asset-warming suites all still green, zero page errors. Screenshots verified the seated pose on the authored swoop and the landspeeder's cockpit.
- 2026-08-29 — **The blade fighter lost her gun and learned to deflect, and the cast took its own names back.** Ventress carries the twin sabers and nothing else now: `ranged: 'none'` builds an empty weapon mount, and her stowed state is empty hands rather than a shouldered carbine, which is what makes every ranged path — firing, ADS, lock-on, peek-fire from cover — switch itself off without a special case at each site, since all of them were already gated on holding the blaster. In exchange the blades **turn blaster fire back at whoever sent it**, at full damage and credited to her. That reuses the block shield's collider rather than growing a second mechanism: `BoltTarget.shield` became a `DeflectSphere` that can carry an aim point (blades throw the bolt at the nearest hostile she is facing, where a pane only mirrors it), a minimum facing dot, and a `consume()` the holder answers, which is where the cooldown and the energy cost live — the projectile system stays ignorant of both. Tuned to read as a parry and not a wall: a ~70° frontal arc so flank shots land, 0.05 s between deflects so one sweep cannot answer a whole burst, and 0.05 of the sprint gauge per bolt, so standing in sustained fire runs her dry and leaves nothing to dash out with. The blades also **put themselves away** after four seconds of no swinging and no deflecting, and come straight back out on the melee button — which now draws *and* swings in the same press, so stowing is never something to undo first. Verified live: she spawns empty-handed, the fire button does nothing, X draws (one blade light) and swings, the blades stow after the lull and redraw on the next press; four spaced bolts into her front all bounce for zero damage at 0.2 of the gauge, the same four into her back all land, and a deflected bolt took 30 off the tusken that fired it. **Names:** the whole playable roster is back to Din Djarin, Paz Vizsla, Bo-Katan Kryze, The Armorer, Asajj Ventress, Embo, Bossk, Cad Bane and IG-11 — the original names were a suggestion that got taken as an instruction — with the docs and the workbench following.
- 2026-08-29 — **The eleven environment textures landed, and all eleven are wired.** The batch the territory audit asked for was the first that was *not* pre-wired by name in code, so each needed its own call: the Ringworld's buildings took `city_facade` as albedo plus `city_facade_glow` as an emissive map (window strips lighting the night side — the facade upgrade a model would have been, on boxes a model was never going to replace), its street signs cycle through `neon_sign`/`_2`/`_3` instead of three of the four flat colour planes, Trask's hulls and decks took `rust_hull`, the Prison Rig's white surfaces `panel_white`, and the Great Forge's dome walls `forge_relief` — on a clone of `ruinMat`, so the carving does not tile across every loose slab and rubble chunk. Three were more than a map swap: the **kelp** is two crossed alpha cards per plant now (the cylinder stays as the stand-in and hides when the frond artwork lands, so the board looks right either way), the **Ringworld skyline** is two parallax rows of silhouette towers beyond each bulkhead, and **cargo nets** hang along the Trask quay edges. All three are `decor`, which turned up a real gap in the audit: the flag was read off the mesh, so a decorative prop built as a *group* of parts (the kelp, now) came back as 45 unbacked meshes. The flag inherits down the tree now, and both audits are clean again. Verified by walking each board's live scene graph and confirming every one of the eleven files is on a real material. The three hunter portraits landed with them and needed no code, as designed; `portrait_ig11` is the last face still drawn as a mark, and is the only open image request besides the environment prop sheets.
- 2026-08-29 — Gave the saber parry its own voice: `saber_deflect` generated (0.48 s, verified decoding in-browser) and wired sample-first ahead of the synth burst that was standing in for it, since a parry can fire several times a second in a firefight and the synth voice was the one part of the deflect that did not sell it.
- 2026-08-29 — **Driving became a vehicle scheme instead of a character one**, on playtest feedback that pushing the stick forward doesn't feel like driving. The left stick now turns the nose (left/right only) and the pedals do the rest: **A accelerates, B brakes and then reverses** once stopped, with W/S as the keyboard equivalents through two new `FrameInput` fields (`throttleHeld`, `brakeHeld`) — which is what lets both idioms coexist, since on foot those fields are ignored and W/S are movement, and in the saddle it is the other way round. The ride's state is now a forward speed along its nose plus a sideways slide, with grip bleeding the slide off, so a hard turn drifts before it bites; per-kind `throttle`/`brake`/`drag`/`turn`/`grip` replace the old single damp lambda, and the bank leans into the steering input as well as the slide. Steering authority is deliberately non-monotonic — 45% at a standstill so a ride nosed into a wall can still turn off it, full bite at a working speed, and 32% stiffer flat out so a top-speed run is a commitment and the boost is a decision. The chase camera eases around behind the nose while driving, but only while the right stick is untouched, so it never fights a look the player is giving it. A is now the accelerator, so **RB is the only dismount** and it reads the speedometer: step off a parked ride, bail out of a moving one (keeping its momentum, popping up into a jetpack chain). The sign the whole scheme rests on is covered by a test: screen-right is -X for a nose on +Z, so a stick pushed right must *decrease* yaw. Controls screen gained a "While riding a vehicle" block — it had also never documented RB's mount, which this closes. Verified with `tools/test-vehicles.mjs` (20 checks): 23.8 m/s under throttle, the brake hauling 23.6 m/s down to a stop and then backing off at 8.2 m/s astern, the nose swinging to (-0.84, -0.54) under a right turn, plus every earlier check and the co-op and loading suites still green, zero page errors. Screenshots confirm the banked mid-turn pose and the camera trailing the nose.
- 2026-08-29 — **The nine environment models landed and are wired.** The whole priority-1 and priority-2 set from the territory audit — `cargo_crate`, `fuel_barrel`, `trawler`, `tram`, `sail_barge`, `freighter`, `cargo_crane`, `reactor_core`, `sunken_transport` — arrived, and unlike the character batches none of it was pre-wired: an environment model has no rig to swap onto, so each needed placing by hand at its site. That swap is the same five lines everywhere, so it went into one helper (`world/props.ts`, `authoredProp`) that loads, places and hides the stand-in, and every board module calls it in one line per prop. Two rules the helper enforces: the procedural build is **hidden, not removed**, so a missing or failed download looks exactly as it did before and the collision audit still sees geometry on every collider; and the sculpts are scaled and placed to fill the audited colliders rather than the colliders being refitted around them. Measuring the loaded models in place — a probe that walks each board's live scene and reports every model's world box — was worth more than looking at them: it caught the two that genuinely did not fit, both of which were colliders drawn around a procedural stand-in rather than around anything real. The **tram** stands 3.78 m to a roof players ride, where the box was 2.6 and would have left them standing inside it, so the box now follows the sculpt (and the buildings' clearance from the tram lane is derived from its width, so widening it cannot quietly reopen the pinning bug that rule exists for); the **freighter** spans 10 m across its wings against a 6 m box, which would have been two wingtips you walk through. Everything else fitted: the sunken transport measured 29.6 × 7.9 × 32.5 against a hull the colliders already described, and the barge, cranes, core, crates and barrels all landed on their bases. One integration detail worth keeping: a shot-out rhydonium barrel hides the mesh `addBreakable` was given, which is the *stand-in* — so the authored drum has to be hidden alongside it, or a destroyed barrel leaves its sculpt standing in the fire. Verified: all nine boards audit clean in both directions, the vehicle, co-op and asset-warming suites pass, and screenshots confirm the cranes, trawler, barge and freighter in place with zero page errors.
- 2026-08-29 — **Shoulder/arm rigging fix and the full animation audit.** Chased the "arms feel tight to the body while running" report to its actual cause: every authored character is sculpted in an A-pose with the upper arm 16–31° out from vertical (mean ~24° — measured numerically across all 24 humanoid `.glb`s), the retargeter pulls that rest to straight-down, and the idle/run clips only gave back 14°/12° of splay — so every arm hung ~10–15° inside the pose its deltoid geometry was built around. Raised the hanging-arm splay (idle 14→21°, run 12→19°), which lands the roster within a few degrees of its sculpted rest; aim and flight clips were already clear. The suspected "shoulder bone too low" turned out true only on the procedural fallback (chest slab to ear level, arm pivot 0.22 m below its top) — documented in the audit but deliberately left alone, since every character ships an authored model and the retargeter never copies bone positions, so the fallback's joint placement is invisible in play. Also fixed while in there: enemy *melee* weapons (gaffi, clubs, darksaber) rode the hidden procedural hand — whose proportions differ from the sculpt's — and floated a hand-width from the authored fist at the top of a swing; they now re-mount into the model's hand on load exactly as player weapons do, while rifles keep the canonical bone so muzzles stay where the firing code looks. The splay sign *and amount* conventions are documented at the top of `clips.ts`. Alongside the fix, a full choreography audit of the clip set — Ventress' twin-saber combo (alternating leads and the cross-slash finisher hold up; what's missing is a saber *stance* out of the swing, footwork, and velocity shaping), the staff combo, the telegraphed enemy swing, the massiff's transverse gallop (well-sequenced but dead-level spine — no vertical travel on the authored path), the krykna's over-mechanical tetrapod, and the strafe gap (side-stepping in combat plays the forward run — a moonwalk) — is written up with a prioritized roadmap in `docs/ANIMATION_AUDIT.md`. Verified: before/after workbench renders across din, ventress, paz, bossk, stormtrooper and tusken in idle/run/melee/saber poses (deterministic frame-frozen captures), `tsc` clean, production build clean, and the full harness suite green (gameplay smoke, co-op seating, asset warming), zero page errors.
- 2026-08-29 — **The last 21 models landed, and every one of them is in the game.** Six needed no work — the three vehicle sculpts (`speeder_bike`, `landspeeder`, `skiff`) and the three weapon props (`saber_curved`, `crossbow`, `longrifle`) were already pointed at by `VEHICLE_DEFS` and `swapWeapon()`, so the files arriving *was* the integration; the weapon props had been recorded as parked by decision, which the sculpts' arrival simply makes moot. Ten were swaps onto geometry that already existed (Nevarro's watchtowers and gate arch, the Forge brazier, the Crevasse crawler, the Refinery's alarm consoles, Trask's harbour shed, the Dune Sea's vaporators, homestead dome and Tusken tents, the Ringworld kiosks), each one line of `authoredProp`. **Five had no home to swap into and were built for the occasion** — the features from PLAN.md §16 that had only ever been a spec: a 35 m **sandcrawler** on the Dune Sea's rim slope, **banthas** at the Tusken camp (solid, so bolts stop on the hide, but no health and no team — livestock, not targets, swaying on their own clock rather than wandering, since a moving collider could shoulder someone into the fire), the **mythosaur skull** half-buried at the Living Waters where the eye already glows, **pipe runs** along the Refinery's blank partitions, and **fish-drying racks** on Trask's quay. Each got a procedural stand-in of its own, because the alternative is a collider with nothing on it the moment a file fails to load. Measuring every model in place — a probe that reports each one's world box — again earned its keep: it found the **survey crawler** is an 8.9 m machine against a 3.4 m box, the **dock shed** reaches 7.1 m to its ridge, and the **Tusken tents** are 5.2 m across a 3.4 m box and taper (so they took stacked discs, like the ice spires); the audit then caught the **sandcrawler**'s flanks half a metre proud of its cylinders and the **banthas**' single disc missing their heads entirely. The **gate** needed pylons of its own — the lintel's collider only ever covered the span above them, leaving the stonework either side as something to walk through. One systemic fix went in alongside: a rider's seat is now **measured off the sculpt** rather than hardcoded, by dropping a ray down the seat column and taking the surface it finds (feet on it for a standing rider, hips a hand's width over it for one who straddles), so an authored vehicle no longer leaves the rider perched above the cockpit the stand-in had. Verified: all nine boards audit clean in both directions, the vehicle (20 checks), co-op and asset-warming suites pass, zero page errors, and screenshots confirm the gate, sandcrawler, crawler, skull and both new vehicles in place.
- 2026-08-29 — **The animation roadmap, implemented, and the whole cast pose-audited.** Every improvement the animation audit called for is now in: **combat strafing** (side-stepping while facing an enemy used to play the forward run — a moonwalk; a lateral shuffle clip and its programmatic mirror plus the run played backward now cover the 45–135° and >135° arcs, picked by facing/velocity divergence, with `cycleDistance` taught to measure lateral sweeps and `gaitRate` taking the character's world scale so Paz strides slower instead of skating); **Ventress' saber stance** (`saberIdleUpper`/`saberRunUpper` — main blade low guard, off blade high behind — picked whenever the blades are lit, so she reads as a duelist between swings, not a courier holding torches); a **saber flourish** one-shot when the combo window lapses with blades still lit; **blade trails** (fading additive ribbons rebuilt from the last ~0.13 s of both blade tips, world-space so the arc hangs where the blade was); **whip-crack melee timing** (strike keys pulled tight behind held windups across all seven combat clips — same poses, real acceleration — with contact times re-checked against the 45%-of-duration hit rule, and the enemy overhead swing's strike now landing exactly on its 0.55 s damage tick); **hit-stop** (55–90 ms attacker freeze on landed melee); **lower-body melee stances** (weight drop/step/lunge one-shots under the three swings when grounded and near-stationary); **directional hit reacts** (flank shots rock enemies away from the bolt with the head snapping toward the shooter, picked by attacker bearing); **massiff gaits** (the gallop finally rises and falls — a spine-root bob authored into the clip with parent-space/world-up conversion — and a new 4-beat walk blends in under 2.5 m/s so it stalks instead of galloping in slow motion); **krykna de-phasing** (the strict two-beat tetrapod rippled into eight offset footfalls plus a carapace bob). A full-roster pose sweep (~140 frame-frozen workbench renders, every character in the poses its kind actually plays) then audited the rest of the cast and caught one more rig-level bug: **enemy rifles floated off the authored hands** in aim poses (worst on the marshal, pirate and capo) — the melee weapon re-mount was extended to every enemy weapon, muzzles travelling with their guns and chest-derived shot direction untouched. Findings, verdicts and known limitations are in `docs/ANIMATION_AUDIT.md` §6–7. Verified: `tsc` clean, production build clean, full harness suite green with zero page errors, and targeted before/after renders for each change.
- 2026-08-29 — **The game is called Bounty Hunters.** The title screen, both page titles, the README, the plan and the defeat banners follow ("The Mando has fallen" was never right once the roster went to nine and only four of them are Mandalorians — solo now reads "The hunter has fallen", a wiped party "The hunters have fallen"). Deliberately *not* renamed: the repository and its Pages URL, `package.json`'s name, the `mando.*` localStorage keys (renaming those would silently wipe every player's saved settings), and the `mando-*.mp3` music filenames, which are real files on disk. Two asset requests opened with it and both are pre-wired, so arriving at the path is the whole integration: **`logo.png`**, a title-screen wordmark — `addTitle` takes an optional art name and swaps the set type for the image on load, keeping the text as the element's accessible label, with the type standing where the file is absent — and **`favicon.png`**, linked from both pages, where a 404 simply falls back to the browser default. Both are exceptions to the docs' standing "no text, no logos" rule, which exists to keep generated art free of invented signage; the game's own name is the one thing meant to be readable. Verified both directions: with no files the type stands and the console is clean, and with a stand-in PNG dropped in, the heading switches to the art and reports its label.
- 2026-08-29 — **Three game modes behind `?modes`: Wave Battle, PvP, Campaign — and bosses.** The title's one Press Start becomes three choices when the experimental `?modes` flag is in the URL; without it nothing changes, which `tools/test-modes.mjs` (now part of `npm test`) asserts alongside a full simulated run of each mode. The design work landed as two documents — `docs/MODES.md` (research: what the existing systems already gave each mode, and the rules built on them) and `docs/LEVEL_DESIGN.md` (the campaign's Gauntlet-informed level grammar, with the pacing math). **PvP:** choose a territory, 2–4 players, free-for-all, three stands each, last fighter standing takes it. Every NPC that reads as a character is playable through an *adapter* over the exact enemy build the wave game spawns (`characters/roster.ts` — no forked character code): stat lanes rebalance them player-side (skirmishers ~100 HP with a 2–3-strong AI squad that escorts its leader, elites with their signature hook, melee heavies), flight only where the in-game version flies, and the whole thing is refereed by making each fighter its own team — the projectile, shield, melee and credit pipelines already resolved everything by team inequality. **Campaign:** a planet strip (nine discs, left to right, off the screen edge, all unlocked for now) leads into a liberation run laid over the territory's own board: a nearest-neighbour tour of its authored spawn posts with squads ramping from wave-1 kinds to elites along it, camp and ambush encounter templates alternating with a breather after each corridor, bacta canisters in corridor pockets and hidden off the golden path, a guide beacon with radar pip and distance line, and two door-gated corridor segments — procedurally assembled interior lanes built 90 m above the territory (the door teleport is the seam real streaming can slot into later) where the enemy cover AI and the player's snap-to-cover finally meet on purpose. All players play on one shared screen: one chase rig follows the party centroid, player one steers it, and everyone's own (unrendered) rig is slaved to its yaw so screen-relative movement and soft-lock aim mean the same thing for the whole party. Checkpoints ride the path; death costs the walk back. **Bosses**, in both wave and campaign: each territory names its warlord — its signature final-wave elite *promoted* (×4.5 HP, ×1.5 damage on every attack path, ×1.3 bulk, knockdown-proof, morale-proof) with a named entrance, a HUD boss bar on every viewport, and retinue calls at ⅔ and ⅓ health through the existing reinforcement machinery. Clearing wave 10 now rings in the boss wave; the campaign ends in its arena. Fifteen end-to-end checks pass headless by stepping the simulation directly (`__manual` + `game.update`), since SwiftShader renders the real game at a crawl.
- 2026-08-29 — **Modes follow-up: mode-aware lazy loading, the PvP VS splash, and the Missions name.** The drop screen now waits on what each mode's first minute actually posts — the wave game's opening wave as before, the campaign's trailhead kinds (its warlord warms in the background with the whole level to arrive), and for PvP only the chosen fighters' own squads, since it posts no waves at all; entering the PvP select also pulls the widened NPC roster's models down on idle bandwidth while players flip through it (`warmPvpRoster`). Locking in a PvP line-up now cuts to a Smash-style **VS splash** (`src/ui/vs.ts`): one angled panel per fighter in their player colour — clip-path slices sharing one seam slant, so two, three and four fighters lay out from the same math — with portrait (the drawn face marks, moved to `src/ui/faces.ts` and shared with the drop screen, replaced by authored `portrait_*` art where it exists), name, kit line and squad size, the VS emblem riding each seam. It plays about three seconds over the match's own warm-up and any press skips it, so the showmanship costs the drop nothing; the pad-driven path (title → PvP → territory → two pads join, ready, start → VS → skip → drop → match) was verified end to end headless with zero page errors. The campaign mode is now presented as **Missions** everywhere a player reads it (title button, planet strip, 'Mission N' stage labels); the internal mode id stays `campaign`. The modes suite grew checks for the splash and the new title labels.
- 2026-08-29 — **A performance pass, and the tab-crashing leak it found.** Profiling all nine boards in the real build turned up one fault serious enough to end a session and one that was simply waste. **The model cache grew without bound.** Its clones are what make a sculpt cheap to reuse, so `markSharedTree` deliberately puts its geometries, materials and textures beyond a match's teardown — right when the cache held two dozen characters that recur in every match, wrong once each territory started carrying a dozen environment sculpts wanted on that territory and nowhere else. Measured across five boards in a row the renderer's live texture count climbed 28 → 45 → 58 → 61 → 74 and never fell; **the sixth board crashed the tab.** `disposeGame` now hands those back through a new `releaseModels(keep)`, which empties the cache of everything outside the playable roster (the menus behind a match are made of it) and disposes each entry's own resources — the bytes stay in the browser's HTTP cache, so returning to a board is a re-parse rather than a re-download. The same nine-board tour now completes with the count bounded and falling as boards change: peak 42, and no crash. **The second finding was where the sculpts were arriving.** A territory starts every one of its models the instant it builds, which on the Dune Sea measured 54 MB across 17 files downloading while the player was already fighting — bandwidth taken from the assets the drop screen genuinely waits on, plus a parse hitch per file. They are now warmed from the character select like the board's textures and enemies already were (`BOARD_PROPS` in `core/prefetch.ts`), which measured all ten of the Dune Sea's sculpts fetched before the match began. Because a hand-kept list beside code that changes goes stale silently, the boards now record what they really asked for (`propsUsed` in `world/props.ts`) and `tools/test-loadperf.mjs` holds the two against each other — as well as asserting the texture count stays bounded across every board, so the crash cannot come back unnoticed. Both new suites are in `npm test`. **What turned out not to be a problem, having been measured rather than assumed:** draw calls are 29–86 per frame across the boards (frustum culling is doing its job on ~600 scene meshes), 198–288k triangles drawn, and the simulation costs 0.12–0.90 ms per update in every mode — wave, campaign and PvP alike — against a 16.7 ms frame. No instancing or batching work was warranted, and none was done. Three newly uploaded textures were also shipping as 5.2 MB of PNG that nothing had wired up yet (`corridor_floor`, `corridor_wall`, `hazard_stripe`); re-encoded to JPEG by the existing tool they are 880 KB, and the tool itself gained a guard it was missing — the planet art is loaded by a CSS `url()` in `src/ui/planets.ts` that names its extension outright rather than probing, so the two opaque members of that set would have been converted and silently 404'd.
- 2026-08-29 — **The logo and the last portrait landed, and the image request doc stopped keeping a diary.** Both files were pre-wired, so integration was verification rather than code: the title screen's heading swaps the set gold type for `logo.png` and keeps "Bounty Hunters" as the element's accessible label (confirmed live — `hasArt: true`, background `logo.png`, label intact), and `portrait_ig11.jpg` closes the drop screen's last drawn mark, with every card on the board now carrying authored art. Verifying that second one needed a fix to the *test*, not the game: portraits arrive on a network round trip, so sampling `has-art` once after the drop screen appears reads false for every card including ones that have worked for weeks — it polls to a deadline now, the same lesson the model intake learned earlier. **`ASSETS_IMAGES.md` rewritten to hold only what is outstanding** (266 lines to ~190): the delivered hunter turnaround sheets and their prompts moved to the history doc where they belong, along with the logo; the weapon-sheet section went with them, leaving one short *Parked* entry for `pistol` explaining why it is not wanted rather than four paragraphs of how it got that way; and the header now names what is open instead of recounting what closed. The A-pose recipe stayed — it is an instruction for the next character, not a record of the last one. Still open and now easy to see: the favicon file, the environment prop sheets and textures, and the game-mode art.
- 2026-08-29 — **The game modes came out from behind their flag.** `?modes` was the switch while PvP and the campaign were being built; they are the default title screen now — Wave Battle, PvP and Missions. The hatch survives rather than being deleted, inverted: `?nomodes` (or `?modes=off`/`=0`/`=false`) puts the single Press Start back and the game is exactly the wave game, which is both what the regression test drives to prove the old path still works and what anyone can reach for if a mode turns out to be broken in the wild. `modesEnabled()` also now defaults to *on* when there is no `location` to read at all, so a non-browser host gets the full game rather than silently falling back. The test that asserted "without ?modes the title is the single Press Start" flipped with it — the plain URL is the three-mode case now and `?nomodes` is the fallback case — and the stale "experimental" framing came out of PLAN §18, MODES.md, the CSS section header and four comments in `main.ts`. README gained a short table of the three modes, since the title screen is the first thing a player sees and it no longer matches the one the README described.
- 2026-08-29 — **The game-mode art landed, and the corridor needed real wiring.** The nine **planet discs** were pre-wired — `ui/planets.ts` builds `planet_<id>.png` from the board id — so arriving at the path was the integration, confirmed with all nine serving 200; prefetch now warms the whole strip when Missions is picked, a couple of screens ahead of it, since nine PNGs filling in one at a time is the first thing a campaign player would otherwise watch. The **corridor interiors** did not: `world/corridor.ts` was flat hull materials, and each surface now takes its tileable (`corridor_wall`, `corridor_floor`, `hazard_stripe`) with the flat colour still standing as the fallback. One trap there was worth the extra line — `mat()` caches materials by colour and hands the same instance to everything built in that hex, so mapping them in place would have put the corridor's deck plate on every 0x383c44 surface in the game; the corridor clones its four first. Verified in a live campaign match: both corridors carry all three maps. The **favicon** arrived too and needed nothing, as designed (200, 512×512). `ASSETS_IMAGES.md` shed the three now-delivered sections and holds only the environment prop sheets and the optional PvP portraits.
- 2026-08-29 — **Bo-Katan benched.** Removed from the game without being deleted: her config, helmet shape, palette, height and `bokatan.glb` all stay, and a `BENCHED_MANDO_IDS` set narrows the *playable* list instead. Everything the game enumerates — character select, prefetch, the drop screen, the `__roster` debug handle, PvP's standard roster — now goes through `PLAYABLE_MANDO_IDS`, so one set membership removes her from all of them at once and putting her back is deleting an id. The model workbench still builds anyone in `MANDO_ROSTER`, so the model stays inspectable. Live check: the roster reads eight, Din through IG-11. Docs alongside: the audio doc's "100 of 100" was stale (it is 111 now — 83 named samples plus four takes each for seven voices, cross-checked against the directory, none missing).
- 2026-08-29 — **The monster bosses got their design, and both asset docs got their requests.** The expansion list's "deeper boss arenas" rung is now a full spec: `docs/BOSSES.md` designs six large creature bosses inspired by the source shows' beasts, one per board that has a monster in its bones — a **mudhorn** loose among the Waystation's cargo, a **ravinak** under the Crevasse ice, the Trask **mamacore** finally surfacing from the pool that has hunted swimmers by the clock since board 13, a **rancor** in Nevarro's town square, a **greater krayt** in the Dune Sea bowl (with the sarlacc as an arena ally, once per phase), and the **mythosaur** rising from the Living Waters to pay off the skull and eye-glow event the Forge has carried since it shipped. They are *additional* to the §4a promoted-elite bosses, not replacements: on a monster board the elite falls first and the monster erupts as a second, final stage with its own banner and bar; the other three boards end at the elite as today. Each fight is specced against machinery the boards already run — hazard zones, breakables, heaving movers, traction, the water system, the light field, `spawnOnHurt` retinues — and the doc keeps an implementation ledger separating reuse from the four genuinely new shared pieces (two-stage boss wave, submerged/emerged state, suction cone, grab-and-carry with its v1 fallback). Every monster carries named weak-zone nodes on the broodmother `sac1..3` precedent (×2 emissive zones against a ×0.5 armored body, ×0 deflect plates on the mythosaur), so the models are gameplay, not dressing. Requests opened in both asset docs: six creature reference canvases in `ASSETS_IMAGES.md` (side/front/top on one canvas, the bantha recipe) and six `loadCreature` model briefs in `ASSETS_MODELS.md` with rigs, budgets (≤ 15k tris, the two partial-body colossi ≤ 20k/2048²) and the weak-zone node contract; the "no new boss sculpts" line in the mode-props section now points at the batch that outgrew it. Design only — nothing in `src/` changed, and nothing blocks on it: the sheets are the blocking input, as ever.
- 2026-08-29 — **Boss battles became an event, in both modes.** The warlord's arrival now opens on an introduction card — letterbox bars, the name in the menus' gold over a red kicker and a "Warlord of ⟨territory⟩" epithet, a generated war horn — while the simulation runs at 0.12× for three and a half seconds and every camera pans onto the boss (`snapToward` eases, so it reads as a shot, not a cut). Inputs are blanked and every combatant is suppressed through the card, so the reveal can't cheap-shot either side. Wave 11 and the campaign arena share the path (`spawnBoss`), so both modes get it for free. The fight itself gained a rhythm: phase turns at ⅔/⅓ break any melee scrum with a damage-free repulsor pulse before the retinue lands, the last third enrages the warlord — a per-instance def copy retunes every movement and cooldown site at once (+28% speed, −40% attack cd, +15% damage) — the bar tints gold→orange→red by phase, and camping at the boss's feet draws a telegraphed shock-slam (ember-ring windup, 26 damage and a 9 m/s fling, armed only when someone is actually close). One CSS trap: percentage heights on the letterbox flex items resolved to zero and the bars silently vanished; flex-basis fixed it. Verified live end to end, including a player parked at the boss's feet eating the slam nine seconds in.
- 2026-08-29 — **The ambient pass finished: the sky got traffic, the pad went live, and the last dressing landed.** `world/traffic.ts` flies procedural liner silhouettes on long elliptical lanes 250–400 m out — blinking running lights, one low lane carrying a doppler-washed `ship_pass` on its close approach — three lanes over the Waystation, two tracking the ring's length on the Ringworld (silhouettes on purpose: at that range a hull is a shape and two lights, and a model would spend a download on detail the eye can't resolve). The Waystation's north-east outrigger became the **working landing pad**: a 100-second cycle of descent on thruster wash, twenty parked seconds, and a climb — the hull rides a Mover, so it's real ground (verified: a player on the roof rode the takeoff 32 m at full health), it shoves rather than crushes anything under the descending hull, and touchdown pings `director.noise`, making a landing cover for a loud approach. The **Refinery's six steam vents** hiss on staggered clocks with plumes through the shared dust pool; **Trask** got its eight buoys riding the exact swell function the sea computes plus rope coils by the bollards; the **Ringworld skyline** got its blinking hazard beacons. Five sounds generated and wired sample-first (`boss_horn`, `ship_pass`, `ship_landing`, `steam_hiss`, `bantha_low` — the herd finally has a voice, on an idle timer scaled by range). Collision audit clean across all touched boards; PLAN §16 is now shipped end to end.
- 2026-08-29 — **Seven waves, two boss battles, and a faster ramp.** The wave game restructures from ten waves + one boss to **waves 1–4 → the board's champion → waves 5–7 → the territory's warlord** (`FINAL_WAVE = 7`, `MID_BOSS_WAVE = 4` in spawner.ts). The champion is a new per-board mid-boss table (`MID_BOSS` in modes.ts): a monster where the board has one (war massiff on the Dune Sea and Nevarro, a blown-up krykna Tunnel Queen on the Crevasse, an alamite Rockdweller Alpha in the Forge), an elite lieutenant otherwise — promoted through the same `promoteBoss` machinery as the warlord but on lighter per-kind scales, a two-body honour guard instead of three, and its own intro card ("Champion of …"). The warlord keeps the full ×4.5 promotion and is the harder fight on every board (roughly double the champion's health). The composition tables compress 10 → 7 with the ramp philosophy made explicit: enemies never get individually harder — waves grow by adding bodies (per-wave growth scaled ×10/7, so wave 7 fields what wave 10 used to) and by debuting kinds sooner; elite caps stay. Ally beats bracket the boss battles (marshal wave 3, IG-11 wave 5, Fennec wave 7). Each wave that debuts a kind now shows a **new-contact card** under the wave banner (`Hud.newContacts`, driven by diffing `waveComposition` against all earlier waves). The campaign gets the same two battles spaced by level progress instead of wave count: the champion's arena at the path's midpoint (a checkpoint on its fall), the warlord's at the end. Tests updated (`test-modes` asserts both battles and victory at wave 8; `test-allies` jumps to wave 3; the spawn audit plans 7 waves); full suite green.
- 2026-08-29 — **The last three models landed: `pistol`, `blast_door`, `corridor_crate`.** One was pre-wired and two were not. `pistol` needed no code at all — `swapWeapon()` in the builder already named the id, so intake was verification: Cad Bane comes up carrying two visible sculpt meshes on the right mount and one on the left, with no procedural weapon left showing, which is the pair working through `weaponMountL` as designed. The two corridor props needed wiring in `world/corridor.ts`, where the stand-ins had been waiting behind a comment since the mode shipped: the door frame now hangs `blast_door` off `authoredProp`, and the cover crates take `corridor_crate`. Verified with every stand-in accounted for — 12/12 crate boxes, 8/8 door posts and 4/4 lintels hidden, 200s on both `.glb` fetches, no console warnings. **The crate is the one sculpt in the game that sizes its collider rather than being scaled into one.** Everywhere else the rule in `world/props.ts` holds — art is dropped into a shape the board already audited — but a corridor invents its crates every run, so there is no audited shape to preserve, and the delivered sculpt measured 22% taller and 50% deeper than the box it was meant to fill: cover you could see over the top of but not shoot over. Collider and sculpt now come off one height using the sculpt's measured proportions, and agree to within a centimetre on every axis and every crate. The door took two passes, both caught by looking at it rather than at the numbers: it went in edge-on, because the sculpt is wide along its own Z where the frame it replaces is wide along X, and once turned to face the approach it swallowed the frame's lit pane behind its leaves — so the pane and its emissive strip are now part of what it replaces rather than kept lit over it. The sculpt carries its own hazard striping and amber lamp, and the campaign's beacon and HUD distance are what actually find the door. The corridor's sculpts also join the campaign prefetch (`CORRIDOR_PROPS`) beside its textures, so ~6 MB of crate and door arrive while the player is still fighting outdoors rather than on the walk in. Verifying any of this needed the polling lesson the intake keeps re-learning: sampling once after the match starts reads *nothing swapped*, because the files are 3 MB each and land seconds later.
- 2026-08-29 — **The six monster bosses landed and became fights.** Requested and delivered the same day, and without the reference sheets that were meant to precede them — so the sheets went from blocking input to not wanted, and the model brief is what a re-export works from now. Every sculpt shipped its brief's rig verbatim, weak-zone nodes and all, and none shipped a single clip: each has a gait authored in code against its own rig (`src/anim/quadruped.ts`), which is the massiff and spider pattern at six times the size. **The fight is a second stage rather than a new system.** On a board with a monster the warlord falling is no longer victory: the ground shakes for four seconds and the thing under it comes up, taking the entrance card, the boss bar, the phase turns, the retinue calls and the enrage wholesale from the §4a machinery — a monster's `Def` already carries its final numbers, so `promoteBoss(name, 1, 1, 1)` hangs the furniture on it and scales nothing. Both the wave game and the campaign arena read one flag (`monsterStaging`) so neither ends an encounter into the quake, and killing the monster is what holds the territory. **The two colossi are posed by the game.** The krayt and the mythosaur are whole animals in their files, and only their front halves are meant to exist: each is sunk into the ground and reared, so the skull, neck and forelimbs stand clear and the body runs away beneath — and while they move they throw the surface behind their heads (dust on sand, spray over water), which is what sells a body passing *through* the ground rather than over it. Those two numbers per monster were not guessed twice over: after two rounds of screenshots disagreeing with each other, the rig itself was measured — every named node's height at five candidate pitches — and the pose solved from the table. The krayt was the one that needed it: pitched the intuitive way it surfaced back-first with its head diving, and its first fitted size made it 31 m long. Verified end to end in `tools/test-monsters.mjs`: six sculpts arriving at their designed sizes and driven by their clips, the two colossi half in the ground with their heads clear, and the Spice Baron falling into a quake that becomes a mudhorn and then a held territory. What is *not* in this round is written down rather than implied — the bespoke movesets and the weak-zone hit volumes are ledgered in `docs/BOSSES.md` §4.
- 2026-08-30 — **Waves arrive now, and nothing can spawn inside a wall again.** Two changes, one bug report. The report — enemies still turning up inside walls and unreachable pockets — came down to two residues in the spawner: `freeSpot`'s ring search asked only "does a capsule fit here", which empty air past a platform edge answers *yes* to (there is nothing there, including a floor), as does a ledge on the far side of a wall or the middle of a burn pool; and `placeBody`'s very last resort returned the raw wanted position, which is precisely a wall when everything else has failed. A spot now needs ground under its feet within reach, no kill/damage hazard, and the last resort is authored ground; the spawn audit passes all nine boards at the stricter standard. **The bigger change makes the question moot for waves 2+: reinforcements arrive instead of appearing** (`src/enemies/arrival.ts`). Each squad comes in whole by the route that suits it — carrier passes streak over the board as jet blurs and release drop squads over their validated posts (staggered a second apart, so a big wave reads as a stream of ships); most bodies fall fast and hit hard, some ride a parachute down slowly enough to shoot and be shot (the canopy is procedural, sways, and is gone the frame they land); quarren surface from the open sea and haul out onto the decks; the boards' own locals and beasts run in over the edge; air squads cross in at altitude. The drop trajectory is aimed, not just simulated: real gravity to a terminal velocity, with horizontal velocity steered to close the remaining gap in the time the fall has left — ballistic to the eye, guaranteed to land on the planner's spot, and a body that somehow misses everything is written off at the kill plane rather than falling forever with the wave waiting on it. Bodies aboard a carrier are owed to the wave (`incomingCount`): the clear check and the hostiles counter both include them, so killing everything on the field with drops inbound does not end the wave. The refinery's roofed posts fall back to walking in, and failing even that to the old instant post, so a wave can never fail to arrive. Two **troop carrier models are requested and pre-wired** (`troop_carrier` for the Imperial boards, `raider_dropship` for the outlaw ones — prompts in both asset docs): without a file the pass is the blur, and once one is cached the pass slows from 85 to 46 m/s and the sculpt replaces the blur automatically. Verified by `tools/test-arrivals.mjs` on the three hardest boards — desert (drops/chutes/runners), trask (swimmers), station (every landing a floating platform): everyone staged arrives, everyone stands somewhere legal, no chute survives its landing, no premature wave-clear.
- 2026-08-30 — **Warlords look and fight the part.** Reported from play: the boss read as one more trooper with a health bar. Three changes: **size** — promotion now grows human-sized kinds ×1.6 and already-big kinds ×1.35 (the Pit Warlord stands 3.5 m over a 1.8 m raider), with the monster bosses untouched since they pass scale 1 and their size is already the point; **the damage read** — a boss flashes red on the emissive channel when a hit lands and pale blue-white when one is turned, on materials adopted lazily (cloned the first time this boss tints them), which keeps the flash off the shared material cache and automatically picks up an authored model that swaps in after promotion; and **the parry** — warlords turn some hits aside with a sharp sidestep off the line of the shot for 15% damage, on a 55% roll behind a 1.2 s cooldown, so sustained fire always gets through. HP scale went 4.5 → 5 alongside. Measured live: 40 spaced shots, 11 turned, 77% of raw damage landed, flash peaks at emissive r=0.8 and restores to exactly the resting values. Warlord-only by construction: the parry flag rides the promotion's hpScale, which the monsters' banner-only promotion sets to 1.
- 2026-08-30 — **The walk-in ally becomes the covert supply cache.** The one ally who used to join automatically on milestone waves is gone; those waves (3, 5, 7 — `ALLY_WAVES` keeps the kind-per-wave table) now drop a glowing crate near the party instead (`src/game/allycrate.ts`). The crate rides the existing breakable pipeline — a physics box plus `addBreakable` with 1 hp, so a bolt, rocket or ground slam from either side springs it — and on opening its four walls and lid get outward velocities and tumble away under gravity, revealing **five** allies of the wave's kind (Marshal ×5, IG-11 ×5, Fennec ×5) who spawn in a ring around the base, join team 0 through the normal escort AI, and are announced with a banner. The backup is temporary: when the wave is decided the surviving squad dust-puffs away (`removeMe`), and an uncracked crate leaves with its chance (collision box and target spheres retired). Until opened, the crate pulses — additive glow shell, emissive panel throb, and a 26 m beacon shaft — so it reads as "shoot me" across the board. Wave mode only for now, matching where allies used to appear. `test-allies` reworked: asserts no walk-in, five marshals out of the first cache, the escort/mill behaviours on the squad, and that the squad melts away when the wave clears.
- 2026-08-30 — **Missions got real levels, and every player got their own camera.** The campaign no longer plays across the open wave arena: each territory now raises a **purpose-built mission level** (`world/mission.ts`) ~90 m over the board — an authored chain of 8 walled fight rooms joined by 7 walkable corridor pinches, per-territory palettes, bend patterns and set pieces (the Dune Sea's sarlacc-maw kill pit, Nevarro's lava channels with a centre bridge, rhydonium barrels on the Spice Run/Docks/Refinery, ice pillars and 0.55 traction over the whole Crevasse footprint, Narkina's shock floors, warlord arenas up to 40×34 m for the monster stage). The genre spine is Gauntlet's chamber chain and Minecraft Dungeons' golden path: **camp** rooms post a garrison you can fight or slip past, **assault** rooms seal both gates behind energy panes and run 2–3 waves from wall vents until the room is held, the champion's arena sits mid-chain and the warlord's ends it. The controller (`game/campaign.ts`, rewritten) ramps squads by *place* along the chain, checkpoints earned ground (a cleared room's far end — never a set piece's centre), teleports anyone who goes over a wall back to the path, and keeps the one-beacon/HUD-distance/radar-pip readability rules. The old door-teleport corridor segments are gone (`buildCorridor` deleted; the door frame survives as the gates' furniture), and mission placement never falls back to the surface — a sealed room waiting on a hostile 90 m below would never open. **The shared centroid camera is cut**: Missions renders per-player split-screen exactly like the wave game (the shared rig framed poorly the moment walls split the party, and cost everyone their own aim); the HUD's shared-screen layout went with it. Verified three ways, and the audits earned their keep: a build audit (no room overlaps, ≥3 validated vents per room, no stranded hostiles) across all nine boards, an **on-foot walkthrough** driving a real player through every room, gate and bend of all nine levels to liberation — which caught a wall-notch wedge at every corridor bend, a floating-crate pocket too narrow for a body, and the beacon standing in the kill pit — and the updated `test-modes` suite (room-chain shape, sealed assault waves, boss retinue at the health marks, liberation, per-player cameras), all green alongside the full build.
- 2026-08-30 — **Preloading became a plan instead of a pile of errands.** Warming was a set of per-screen calls, each hand-picking files and a priority, and the ordering between them was whatever the call sites happened to agree on. It is now one thing: every screen *declares* what it is made of, and `warmFor(context)` — told where the player is standing and what has been decided so far — walks the route ahead (title → territory grid or planet strip → character select → drop → match), giving the screen on show `now`, the next one `soon`, and everything past that idle time. `setState` in `main.ts` runs it on every transition and is the only caller; the character select re-runs it on every flip through a new `onBrowse` callback. Two rules carry the design: **a screen only declares what it knows** — there is no board in the plan before a territory is picked, no drop-screen cast before there is a board whose hostiles it can name — so nothing speculative is ever queued; and **certain is not the same as soon** — a need marked `soft` (the rest of a roster, the waves after wave one, corridors an hour into a campaign) rides the idle lane whatever screen declared it, so seven character models nobody is looking at cannot outrank the files the very next screen blocks on. What actually changed on screen: **the title now pulls the nine planet discs as well as the nine territory cards** (Missions was warming its strip only once picked, so the strip was still the first thing a campaign player watched fill in), and **the drop screen's portraits are warmed from the character select** — they never were, so every face on the one screen built to be looked at popped from drawn helmet to photograph a beat after it appeared. Ranking moved too: whoever is on a plinth and the two either side are the select's `now`, the drop's sky and wave-one models its `soon`, and the territory's ground, sculpts and later waves fill in behind them on idle time, where before they competed as equals. Two systemic fixes fell out. `preloadAuthored` is gone: it fetched straight at the network with no cap, so flicking through a roster (or a six-kind wave landing) opened a download per model and left the one being waited on queued behind them — everything now goes through the capped warm queue with an urgency, `now` included. And `portraitName` moved into `core/assets.ts`, because the prefetcher and the drop screen have to agree on the filename or the warm request silently misses; the drop screen and the VS splash both take it from there. Verified with the full suite green (`tools/test-loading.mjs` gained three checks — the planet discs warmed from the title, and the drop screen's own and hostile portraits warmed from the select), `tsc` and the production build clean, zero page errors.
- 2026-08-30 — **Some transports land now.** Where the ground near a drop squad's posts is open and flat — dune clearings, deck plates, landing pads — every other carrier sets down instead of overflying: it comes in decelerating, descends onto the spot, sits two and a half seconds while the squad steps off around the hull and walks to its posts, then climbs out and leaves. `landingSite` does the vetting (a 6 m footprint of real ground, under 2.2 m of height spread, nothing standing in it, clear sky, no hazard — with a ring of nearby clearings tried before landing is ruled out, since the squad can walk a few strides), and ineligible squads simply keep the flying drop, so nothing is ever owed a landing the board cannot host. The blur streaks fade with airspeed, so one visual reads as a streak in the pass and as a ship on the pad; `audio.shipLanding` — generated for the Waystation's ambient pad and waiting for a second consumer — plays the flare. The requested carrier sculpts were generated without landing gear (deliberately: "in level flight"), so the game slides **subtle procedural skids** under the hull for the parked beat, added after the model-swap snapshot so they survive an authored hull arriving. Covered in `tools/test-arrivals.mjs`: a transport sets down within a wave or three on the desert, and its squad walks off, takes its posts, and stands somewhere legal.
- 2026-08-30 — **The troop carriers landed, and the pre-wiring held.** Both `troop_carrier.glb` and `raider_dropship.glb` arrived hours after being requested and needed zero new code: `loadProp` swaps the sculpt over the blur stand-in, `authoredCached` flips the pass from an 85 m/s streak to a 46 m/s flyby, the streak planes fade with airspeed, and a landing transport parks the hull on the procedural skids (which were added after the swap snapshot for exactly this day). Both sculpts are long along Z as briefed, so the `axis: 'z'` fit was right first try. Verified in-game on the dunes: a parked `troop_carrier` with its stand-in and streaks hidden, squad walking off, another pass crossing behind. The reference sheets, requested as the first pipeline step, were overtaken by the models themselves — marked not-wanted, prompts kept as the design a re-sculpt must match. One test-craft lesson repeated itself: a synchronous stepping loop starves the event loop, so a probe that steps the simulation while waiting for a .glb must yield between batches or the load never lands.
- 2026-08-30 — **The deploy was two blockers deep, and PvP learned that a fighter can be four metres long.** The live site had been stale since 2026-08-29 20:13 — no mission levels, no per-player cameras — while `main` carried both. Neither cause was the code. First, `test-allies` budgeted **animation frames** where it meant game time: the same 400-frame loop is 20 s of match at 20 fps (dt is clamped at 0.05) and under 7 s at 60, so "the squad melts away when the wave is decided" passed on slow local software rendering and failed in CI on faster frames — and since waves arrive now, deciding one waits ~7.4 s on a landing transport's ramp after a per-pass stagger. Its sibling check had the same disease from the other end: it held a hostile in an ally's face for 25 frames, but five marshals kill one in about a second, so it swung between 5/25 and 24/25 on frame rate alone. All three checks now step `game.update` directly (and keep the foe alive, since whether the ally could kill it was never the question) and pass 90/90 and 70/70 across repeated runs. Second, and hidden underneath: the job's `timeout-minutes: 20` had been outgrown by a suite that is ten browser suites — `npm test` ran 19m17s, was cut off, and every step after it was skipped. **A job killed on the timeout reports as `cancelled`, which reads exactly like a run superseded by the next push**, which is why a day of deploys looked merely skipped rather than broken; the budget is now 45 minutes of headroom. **And the PvP size audit.** Measured rather than guessed, across the whole roster: heights are a sane band (1.2–2.7 m) but footprints are not — `npc:massiff` is 3.2 × 4.9 m and `npc:broodmother` 6.5 × 6.1 × 3.9 m, against a select line spaced 1.9 m at four players and a chase rig whose every constant (`BASE_DIST`, the 1.58 m eye) is tuned around a 1.8 m Mandalorian. The collider never said so, because the roster clamps a playable NPC's capsule to 0.6 × 2.1 so it still fits the cover and doorways the boards were built around. So the beasts overhung their neighbours on the plinth, and in play the camera sat *inside* them — the massiff's player got a wall of hide and no view of the world, confirmed by a probe that reported `cameraInsideBody: true`. The select now scales a fighter onto its plinth and the camera is told the body's real height and reach, both measured off the built model through one shared `core/bounds.ts` (visibility-aware, because `Box3.setFromObject` unions a body with the hidden procedural ghost underneath it) and both re-measured when the authored `.glb` swaps in at its own size. A Mandalorian measures the reference exactly, so the framing the eight of them had tuned by hand is unchanged — verified at 2.31 m against 2.30 before — while the massiff pulls to 4.06 m and the broodmother to 6.15 m, every body outside the lens. Neither change touches how big anyone fights.
- 2026-08-30 — **Ventress's saber throw on RT.** The trigger — dead for a fighter with no gun — is now her signature: a pull sends a lightsaber out along the crosshair (soft-locked onto whoever sits in the assist cone), spinning flat like a thrown disc with the blade trail riding it, and it keeps flying for as long as RT stays down — stalling to hover where it meets a wall or its 30 m range — then comes home the moment the trigger lifts, pulled straight through anything in the way. A second pull while the first blade is still away throws the other hand's saber, so both can be out at once. The blade circle cuts what it sweeps through, out and back alike, on a 0.4 s per-target beat (full melee damage per cut, saber kill = the usual melee fuel refund). The cost is her hands: each thrown blade vanishes from its hand (`setSaberHeld` on the character, a per-hand mask inside `showWeapon`), and with both gone she fights bare-knuckled — 0.4× damage, 1.8 m reach instead of 3, shorter lunges, staff-whoosh audio instead of the saber sweep — and, the real price, **no deflect**: the blade collider returns null with empty hands, so a thrown-out assassin eats blaster fire she would normally bat back. Throws are blocked from cover, the saddle, underwater and mid-swing, but blades in flight keep flying on every path — she can block, ride, swim or die (they come home to the body) — because the flight ticks before any of update()'s early returns. Implemented as `ThrownSaber` (`src/player/saberthrow.ts`): a scene-level spinner group per hand, main hand carrying the one blade point light, same budget rule as in hand. Verified end to end with a harness probe: throw, second throw, empty-hand deflect refusal, bare-knuckle numbers, recall and catch.
- 2026-08-30 — **The super jump: flight for everyone who doesn't wear a jetpack.** `PlayerProfile.canFly` became `flight: 'jetpack' | 'superjump'` — helmeted Mandalorians keep the fuel-budgeted jetpack loop untouched, and every other playable (the bare-headed hunters Ventress, Embo, Bossk, IG-11 and Cad Bane, plus the whole PvP NPC roster) flies by holding A instead: the take-off press arms the climb, and as long as that same hold lasts she keeps rising at ~9 m/s — as high as wanted, no fuel, no flames. The moment A lifts (or the shield comes up) the climb is spent for good — nothing relights mid-air — and re-pressing on the way down only *feathers* the fall (gravity at 0.35×, fall capped at 5.2 m/s), with flyer-grade air steering during both the rise and the glide. The JET fuel bar hides itself on a super jumper's HUD the way the HEAT bar already hides without a blaster. This is strictly the *player adapter*: AI-driven enemies keep their own flight logic, so a hostile darktrooper flies exactly as before — only a person playing one super-jumps. `tools/test-modes.mjs` updated (a playable NPC now asserts `flight: 'superjump'` while the Mandalorian keeps `jetpack`), and a harness probe verified the rise, the spent climb, the feathered fall and Din's unchanged fuel burn.
- 2026-08-30 — **Jetpack descent control, and the air dodge earns its direction.** Two additions for the fliers. (1) A quick airborne **A tap** (released within 0.22 s, classified on release so a real thrust hold never flips it) toggles a Mandalorian between normal falling — still the default, and the state every landing resets to — and an **eased descent**: the pack idles against gravity at a visible low burn (`thrusting = 0.35`, so the flames, the hum and the flight pose all come along), gravity at 0.3×, the fall capped at 4.5 m/s, for a slow sip of fuel (a fifth of the climb rate); an empty tank drops you back to a real fall until it recovers. (2) The **LB dodge now arms in the air the way it always did on the ground**: press LB with the stick centred and the dodge waits for a direction, firing the instant one arrives — a falling Mandalorian holds LB and flicks right to dart right, momentum-frozen (`vy` clamped ≥ 0 through the dash) mid-fall. Previously an airborne LB press with the stick centred fired camera-forward on the spot, spending the dodge before a direction could be chosen. Verified with a harness probe: thrust climb, tap-on (fall −4.5, fuel sipped), tap-off (fall −15.8), a 0.5 s hold leaving the mode alone, LB armed-but-waiting in the air, the sideways dash at 19 m/s with fall frozen, and the landing reset.
- 2026-08-30 — **Four play reports: everything ragdolls, the hands sort themselves out, the station is a vacuum, and the sculpts carry their own colliders.** **Deaths.** A creature on a free-form rig — the krykna, the massiff, the interceptor drone — had no canonical skeleton for the articulated solver, so it died by a canned tip onto its flank: the same fall every time, always ending flat on the spot it died. `RigidRagdoll` (src/anim/ragdoll.ts) gives them the same Verlet world as the humanoid solver with the constraint pass replaced by shape matching — eight corner points fall, hit things and lose energy independently, and each step the cloud is snapped back to its rigid shape rotated to fit, the rotation recovered with Müller's iterative polar decomposition. The body tumbles, drapes over what it lands on and falls off a ledge it died on. Two things had to be got right: collision and shape match run once per step in that order (iterating the pair ratchets, and the corpse climbs the ground it is lying on), and rest is "slow while touching something" rather than "stopped", since a rigid body settling on its corners never goes perfectly still. `PhysicsWorld.pushOutPoint` is the loose-point contact the sim needs.
**Loadout.** The button that uses a weapon is now the button that draws it: X swings and the blade comes out to do it, RT (or LT) fires and the gun comes back up — including straight out of a combo, which used to leave the trigger dead until the player found the swap button. The D-pad no longer toggles between the slots; it picks *within* one — left cycles the blades, right the guns — and every character carries both, so a fighter is never one press away from being unable to do the other thing. `MandoConfig.ranged`/`melee` take a list, `buildMandalorian` mounts every weapon in the loadout and shows the pair in hand (the muzzle belongs to whichever gun that is), and `PlayerProfile` carries `rangedOptions`/`meleeOptions`. Din picks up the darksaber beside the staff. Two fighters keep an empty gun hand on purpose, and neither is short of a ranged answer: Ventress' RT is her saber throw (landed upstream the same day), and the playable war beasts have teeth.
**Gravity.** "The Spice Run" is a constellation of platforms in deep space and it pulled everyone down everywhere at a flat 0.45 g, so flying between islands was a fight against a floor that was not there. `Board` takes an optional `gravityAt(x, y, z)` field; the station's is 0.05 g in the open and the old 0.45 over anything you could land on, blending between five and eighteen metres above it (`PhysicsWorld.supportBelow` answers "is there anything under this spot"). Fighting on a deck is unchanged to three decimal places. The void under the board and scripted drop arrivals keep the flat scale on purpose — a local field is about where you can land, and a squad coasting in on 0.05 g would still be in the air two waves later.
**Colliders.** Every environment sculpt replaces a procedural stand-in, and the colliders were placed around the *stand-in*: the station crane was a mast and a boom, so the cab, the counterweight and the slung container it actually draws were all air; the freighter was a box around a hull, which is the loose shell you sink into walking its spine; and a crate set down at an angle had an axis-aligned box that missed its corners entirely — a third of the sculpt with nothing under it, on four boards. `world/collide.ts` fits colliders to the model instead: the sculpt's triangles are rasterised into a coarse voxel grid, thin hollows are closed, the cells are greedy-merged into as few boxes as they will go, and those replace the stand-in's the moment the model lands (a board whose art never arrives keeps exactly what it had). `Mover` learned to carry a fitted set, so the landing freighter and the heaving trawler get theirs too. `tools/audit-props.mjs` is the other half of `audit-collision.mjs` — it waits for the sculpts and measures both directions of the mismatch, holes (fly through it) and slop (stand on air) — and is what found the rotated crates. Every board now passes it; the collider count roughly trebles on the station and the whole physics step for forty bodies still costs 0.28 ms. Three new tests join the suite: `test-ragdoll`, `test-loadout`, `test-gravity`.
- 2026-08-30 — **The cast is right-handed, because the rig finally is: `L` moved to +X.** Reported from the workbench: the authored model and the procedural build stood as mirror images, "strafe left" carried each of them the opposite way, and clicking a joint on the model selected — and moved — the far side of the body. One root cause, three symptoms. The canonical rig faces +Z, so +X is the character's own left, but `skeleton.ts` put the `L` bones at -X: every side label named the opposite side. The authored Rigify skeletons label theirs correctly, so `authored.ts` had reconciled the two by mirroring the source pose through X=0 (`q → (x, -y, -z, w)`) on its way into the model. That won the blaster its right hand and lost everything else: a world-space reflection makes the model strafe away from its travel, lean out of its turns and roll its chest the wrong way through a run, and it left the driving rig's bones — which is what the editor's joint handles hang off — mirrored against the skin drawn over them. **Fixed at the root: the rig is flipped, not the pose.** `L` bones sit at +X and `R` at -X, and every clip key is mirrored to match (Y and Z negated on all 637 rotation triples; X is the sagittal axis and untouched, and the hips' position tracks are all on the centreline). That is a true reflection of the whole cast, so each body keeps its own anatomy and simply becomes right-handed — which is what the camera had assumed all along, having always framed over the anatomical right shoulder while the gun sat in the left hand. Everything that reads a side went with it: the strafe cycle and the flank flinch swap which variant answers which bearing (`strafeLower` is now genuinely the step to the right, `hitFromR` genuinely a bolt from the right), the procedural detail that isn't mirror-symmetric follows the body (bandoliers, the deathtrooper's lone pauldron, the officer's rank plaque, the tusken's pouch, the cape mount, the swoop rider's hand-set seated pose), and the retargeter drops `mirrorX` to map the two skeletons name for name. Verified in the workbench: `DEF-hand.L` and the rig's `handL` now sit on the same side of the body (+0.37 m and +0.53 m off centre), and the two figures hold the same pose, in the same hand, through idle, aim and both strafes.
- 2026-08-30 — **Three workbench edit-mode bugs, all downstream of a pose that was never written.** (1) **A rotation snapped the figure to a neutral stance.** Editing a bone the clip has no track for adds one, which needs the mixer's actions rebuilt, and `invalidate()` restores every bone to its bind pose on the way through; the pose was then replayed with `play(..., fade = 0)`, and a *zero-length fade* is not a cut — the mixer's weight interpolant reads 0 at the instant it starts, so the clip contributed nothing to the `update(0)` that follows and the bind pose was what stayed on screen. `Animator.play` now cuts to full weight when the fade is zero instead of scheduling one. (2) **Undo left the joint where it was.** Dropping such an edit drops the added track with it, and a bone no clip animates has nothing to write it back — it kept the rotation for the rest of the session. The workbench now snapshots each rig at build time and returns to that rest before playing a pose, which is also what stops a bone the *previous* pose animated from carrying over. (3) **Every drag flickered through the bind pose**, because rewriting a clip meant new `KeyframeTrack` objects and therefore a rebind. `PoseEdits.apply` now writes sample values into the track's existing array, which a playing action reads through its interpolant immediately, and reports "structural" only when a whole track is added or removed — the one case that still needs `invalidate()`. Verified by driving the real page: a 30° `shoulderL` edit (the no-track case) holds while the rest of the pose stays put, undo returns it to 0 and empties the ledger, redo restores it, and the edit survives leaving edit mode and a pose round-trip; two stacked edits undo and redo in order.
- 2026-08-30 — **Fliers you can see, and a fall you can shoot from.** Reported together from play, and only one of the two was actually broken. **Air combat already worked**: measured against the ground, a player in the air fires the same six shots a second and raises the shield fully — nothing gates shooting, aiming or blocking on being grounded, and the aim pose and combat facing already play mid-flight. What was missing is the reason to try: a plain fall is 18.6 m/s of ground rushing up, which is no place to line anything up. **The aim-glide** puts the boosters under you instead — aiming in the air with the jetpack off eases the descent to 1.9 m/s (a tenth of the fall), bleeds the drift off with it so a shot holds its line, burns the tank in about seven seconds, and plays a quarter-power plume so the pack is visibly doing the work. It replaces gravity rather than resisting it, which is the whole trick: the first cut damped the fall *against* a full g and settled at 7.6 m/s, because a damp and an acceleration just find the speed where they cancel. A dry tank does not glide — and the airborne trickle-refill had to stop while the sights are up, or it handed back a sliver of fuel the instant the glide cut out and restarted it, a frame at a time, all the way down. **Skyline halos** (`src/fx/skyline.ts`) answer the other half: a flier is always seen against the sky, and a body the same tone as that sky is a silhouette against itself. A faint disc rides behind the body, opposite in tone to the board's sky, and the body — already in the depth buffer — punches its own shape out of the middle, so what is left is a rim rather than a blob; it fades in with height over whatever is directly below, so it appears exactly when a body is skylined and never haloes anyone standing on a deck. It is a contrast-deficit remedy, not a marker: **only** a dark body on a dark sky (a light rim) or a light body on a light one (a dark one), and nothing at all for a body that already reads. Judging that needed the body's real tone, which means reading its *texture* — an authored trooper leaves `material.color` white and carries everything in its map, so the map is scaled to 8×8 and averaged once per texture. The first metric compared body and sky luminance directly and got the Dune Sea wrong, because a board's background colour is a base tint and the rendered sky carries sun and haze on top of it; asking instead how far the body sits into its sky's *half* of the scale needs only the half, which the estimate names reliably. Covered by `tools/test-airplay.mjs`. The glide landed the same day as the eased descent above and the two are deliberately separate rungs of one ladder: the aim-glide is the strong one and takes the frame when the sights are up (1.9 m/s, a seventh of the tank per second), the tapped `slowDescent` idle is the cheap one below it (4.5 m/s, a sip), and only one of the two ever governs `velocity.y` — the glide *replaces* gravity where the eased descent scales it.
- 2026-08-30 — **`main` did not compile, and two sessions rewrote the same death test.** Reconciling the flier work with the jetpack and super-jump work beside it turned up a tree that no branch had broken on its own: the boss super-jump branch and the local-gravity branch each merged cleanly and left `grav(game)` called with one argument against a two-argument signature (`src/enemies/enemy.ts`, the ballistic leap). Two call sites. A clean textual merge of two green branches is not a green tree, and `tsc` is the only thing that says so. **The other half is a lesson in how far a wrong measurement can lead you.** `test-ragdoll` was failing about a third of the time here, and chasing it produced a diagnosis that was self-consistent and wrong: that a massiff comes to rest on one end because `RigidRagdoll` contacts the world through a collision capsule clamped to fit the boards' doorways (1.7 m) while the animal is 5.5 m long, so a nearly-cubic box cannot tell a flank from a nose. Three fixes were built and measured against that theory and every one was worse — sizing the box to the drawn body makes an end-up rest *more* stable, biasing the initial spin about the long axis is outlived by the tumble, and a righting torque either stands the animal back on its own feet or lands every corpse identically on its back. All of it was reverted. The actual cause, found in parallel and taken wholesale (`74096ea`), was in the test: it killed with `damage()` alone, where the game's bolt runs `damage()` *and then* `knockback()` — and knockback is what puts speed into the body, which is what the ragdoll seeds its spin from. So the check was really asking whether the creature happened to be charging as it died. The moral is not subtle: **a defect reproduced only through a harness is a defect in the harness until the game's own path reproduces it**, and a theory that survives three failed fixes has earned doubt rather than a fourth. What did survive from this side is in that file by its own author anyway — fixed-dt stepping over wall-clock waiting. **One finding is left standing for whoever picks the test up next**, measured after the real kill path was in place, so it is not the harness this time: the check reads the *root origin*, and that origin stops meaning "the creature's feet" the moment a solver poses the body — a trooper lying correctly flat reports −0.8 m, a massiff on one end +2.1 m, and a tolerance wide enough to admit both would also admit a body buried to its neck. Measuring the lowest point of the drawn geometry instead (visibility-aware, since a hidden stand-in's meshes keep their own `visible` flag) puts krykna and stormtrooper at a tidy −0.2 m across every trial — and the massiff at −1.2 to −2.7 m, consistently, in most runs. That is a five-and-a-half-metre animal settling well inside the sand, and it is the same mismatch from the other end: `RigidRagdoll` contacts the world through a collision capsule clamped to 1.7 m so the beast still fits the boards' doorways. Worth a look with fresh eyes; three attempts from here (box sized to the body, spin biased about the long axis, a righting torque) each made it worse and were reverted.

- 2026-08-31 — **Six play-report fixes, four of them in Missions.** A Missions board opened on a chorus of grunts: the level posts its whole garrison at construction and every body played its arrival bark at once, so two dozen hostiles standing quietly three rooms away all announced themselves over the load. Arrival barks are now opt-out (`Enemy` takes `{ silent }`) and a posted garrison is silent — it is *already there*, which is not the same event as arriving. **Doorways became doors.** Every mission doorway now carries a real blast door: two leaves parting over 0.75 s in a frame trimmed in the territory's accent, filling the full-height opening rather than the frame's 3.6 m, with the blocker held until the leaves have actually cleared — so a door is never passable while it still looks shut, and nothing is shot through one. Shut is the resting state; what opens them is progress (rooms behind the party stay open, the room being approached opens the door you walk in by, a camp keeps both open, everything ahead stays shut). Their animation is driven outside the match's `fighting` state so a door caught mid-slide by a boss intro does not freeze holding its blocker. **A sealed room waits for the party.** The seal fired on `anyInside`, and it is the same door everyone walks in through, so the first body through locked the rest out of their own boss fight; it now waits for every player still standing (the dead excluded — they return at the checkpoint, and a wipe would otherwise stall the level for good). **Players flash red when hit**, per-instance: an authored sculpt hands every instance the same material objects, so the materials are cloned per fighter first — reddening a hit player would otherwise redden the rest of the party and every hostile wearing that model. Colour, not emissive, which `setHeroLight` already owns.

  **Corpses stopped standing up.** Reported as tuskens sticking up diagonally, and the measurement went through three wrong answers before the right one: the corpses' bones read flat (head 0.18 m, hips 0.15 m above ground) while the render showed bodies upright to the waist. A `SkinnedMesh`'s `geometry.boundingBox` is its *rest pose*, so measuring bounds answered a question nobody asked; and the first probes had killed bodies before the authored `.glb` landed, measuring the procedural stand-in instead of what actually renders. Waiting for the sculpt and reading its own skeleton put the bug where it belonged: the articulated `Ragdoll` judged sleep on speed alone, so a body fell asleep at the **apex of its knockback arc** — every point momentarily still — and froze there, upright, legs in the sand. Only the throws that peaked quiet did it, which is why it looked random. `RigidRagdoll` next door already had the right rule and the articulated solver now shares it: asleep means slow *and* touching something. **The colossi are pinned to the ground.** A `plows` body swims through the surface, never jumps and never falls, but a three-metre capsule against a one-metre wall can be squeezed out by a push-out that picks the outside face — and off a mission plate that is a 130 m drop onto the kill plane, which deletes the boss and leaves a room that never clears. It is now glued to the surface each step, and any hostile that does leave the plate is put back on the arena rather than lost, the same catch the party already had.

  Verified: `test-modes` gains four checks (a door ahead of the party is shut, nothing shoots through it, a sealing room stays in `travel` with one player inside and seals at two) and its campaign walk now targets room centres rather than the threshold, where the blocker stands — plus a guard cut from 300 turns to 60, so a stall fails in a minute instead of spinning for twenty. Full suite green.
- 2026-08-31 — **The warlord brings his own music.** `mando-boss.mp3` takes the music bus the moment the final boss card comes up, on every board, and holds it until the fight is over — including the monster stage, where the warlord going down is an interval rather than the end — after which the board's own rotation resumes. The champion's battle at wave 4 deliberately keeps the board score, so the change at the last fight means something. The track sits outside every playlist (`MUSIC_BOSS` in `src/core/music.ts`, `audio.startBossMusic`), so it plays for the boss and nothing else, and it falls back to the board's music rather than to silence if the file will not load. Verified live on the Great Forge: the board opens on `mando-fada.mp3`, the warlord's arrival pulls `mando-boss.mp3`, and killing him with the field still hostile brings `mando-fada.mp3` back.
- 2026-08-31 — **Seven play-report fixes: mission drops, corpses, blades, guards, hits and the select-screen pictures.** (1) **Missions waves arrive by transport.** A room's waves used to be built beside the wall vents; a mission level is a chain of walled rooms with the open sky for a ceiling, so the wave game's carrier pass (`src/enemies/arrival.ts`) works over it unchanged — the vents still choose where the squad ends up, but it gets there by ship. The room is held open across the flight (`Campaign.dropping`), or the clear check would find an empty force the frame after the wave was called and open the doors on defenders still in the air. No parachutes indoors: a canopy takes seven seconds to cover the drop. (2) **The tuskens left standing were two bugs.** A rigged enemy that died while its knockdown timer was running skipped the solver entirely and kept the pose it was holding — and in the first fraction of a second after being knocked over, that pose is still standing. The question is now asked of the body (`Enemy.torsoUp`) rather than the flag. Separately, an upright stack of Verlet points with its feet on the ground is a *stable* arrangement of distance constraints, so a corpse that happened to balance there stayed balanced; a fading sideways push while a body is still on its feet (`TIP_ACCEL` in `src/anim/ragdoll.ts`) takes that equilibrium away. Both measured on the drawn sculpt, not the procedural rig. (3) **The thrown saber is never thrown downhill** — the aim assist happily pointed it at the dirt when a short enemy stood on lower ground — and (4) it **turns for home on its own** once it has spent its range or met a wall, instead of parking in mid-air. (5) **The guard gap was real and measured**: the saber parry answered only a ±69° cone, and its gauge emptied after about twenty bolts, so sustained fire from straight ahead started landing while the character was visibly swinging through it. The arc now reaches past the shoulders, and a live swing turns bolts for free — the gauge pays for standing with the guard up, not for fighting. (6) **IG-11 projects a closed bubble**, centred on the droid and answering from every bearing including dead astern. (7) **A hit is an event**: the body flashes, takes a shove away from whoever threw it, and cannot be hit again for a third of a second, so a volley reads as a countable sequence instead of one smear ending in a death screen; a fresh body gets a longer window and pulses cold while it holds. Fire and hazards are exempt at both ends, so lava still kills. And the **select-screen pictures now show the pose the model stands in**: `poseAt` moved the clip's clock but never finished its crossfade, and a mixer update of zero seconds spends no time, so every poster was shot with the idle at no weight at all — the rig in its bind stance, arms flat at the sides, beside a live model standing with its arms relaxed out. The fade is finished there now, the plinth holds the body at the picture's own frame until the handover, and all thirty posters are regenerated. Two new regression tests (`tools/test-block.mjs`, `tools/test-hits.mjs`) and new checks in the arrivals and ragdoll suites.
- 2026-08-31 — **The broodmother's clutch is on her own eggs now, and the brood hunts.** Three reports, one root cause between two of them. **The rack reads six because the sculpt has six.** `krykna_brood.glb` carries six eggs on her abdomen and the game was drawing three of its own beside them, so the back had nine things on it and none of them agreed. The six now sit *on* the sculpt's eggs, measured rather than guessed: the sac bones' vertices were pulled out of the decoded mesh, a dome fitted to the abdomen, and what stood proud of it gave the region — then four rounds of place-render-compare closed the last few centimetres, because the mesh is a single meshopt-compressed skin on one material and neither skin weights nor triangle connectivity nor texture colour will isolate six touching blobs from it. Black when spent, two blue flashes as one charges, white when it is ready to lay. Two things fell out of the rack being a *readout* rather than skin: it stays hidden until something drives it, since the same body fights as a wave boss where nobody counts her eggs and an undriven rack would bury the sculpt's pale clutch under six black balls; and the player's hit flash now skips anything marked `userData.readout`, because that flash **clones every material it finds and swaps the clone onto the mesh** — which is why the first cut of this shaded nothing at all, the builder's captured materials having been quietly orphaned. A readout must write to the material the mesh is wearing now. **The brood hunts.** A hatchling was a squad follower, and the escort AI anchors a follower to its leader and only engages what strays within `ESCORT_ENGAGE` of *her* — so laid spiders trotted at her heels past anything worth biting, which is not what laying them is for. `owner` now carries a posture: a follower escorts, a hunter takes the same team and the same kill credit and goes after the enemy on its own, with no sight gate (the brood is one animal with the mother, and she can see the whole board). Measured: a hatchling crosses 42 m to the rival and ends 1 m off it, 45 m from its mother. **And nothing caps the swarm but her laying rate** — the old ceiling of eight living brood made the ninth egg silently do nothing, which reads as a broken button rather than a limit; the ration is the three seconds an egg takes to grow. A queen who spends the match laying now fields 26. Covered by `tools/test-brood.mjs` (12 checks: the rack's three states, the hunt, the swarm past the old cap, and the AI boss wearing no readout).
- 2026-09-01 — **Her egg sacs inflate and deflate now, not just change colour.** Asked for on top of yesterday's readout, and the interesting part is what had to move to allow it. A sac could not simply shrink: the sculpt's own six eggs sit underneath the six the game drives, so anything smaller than the covering size revealed a bright sculpted egg sitting in the socket the game was calling empty — measured, and it looks exactly as wrong as it sounds. Growing the full state instead was no better, since the six already overlap at full size and enlarging them merged the clutch into one blob. What works is retiring the sculpt's clutch outright: it hangs off three named `sac` bones, and **winding those bones to 0.01 takes the eggs off her back and leaves the abdomen intact** — the body is skinned to them only lightly, so the animal is unchanged but for a couple of vestigial nubs at the rear. That is done only once something drives the rack, so an AI broodmother (she is also a wave boss) keeps the clutch the sculpt gave her. Freed of the thing underneath, a spent sac is now a slack shell at 0.38 of full size and flattened to 0.55 of its own height, swelling and paling in step as its egg grows and collapsing the moment the egg leaves. Filling eases at 3.5/s so it tracks the three-second charge rather than racing it; emptying at 7/s, because the egg physically left her back. The eased fullness lives on the character's `cosmetic` tick, which is also where the sac bones are re-wound each frame, since whatever poses the rig owns them. Two smaller corrections came out of looking at it: with the textured sculpt eggs gone, pure white read as six plastic balls, so the ready tone is the sculpt's own warm cream at a rougher finish and the spheres came down to the size of the eggs they replaced. **The test learned something too.** Catching the rack at a true zero in play is not possible — an egg goes on charging while the last ones are being laid, and one finishes during the beat it takes the sacs to collapse — so chasing that produced three checks that failed on timing rather than on behaviour. The swell's *shape* is now measured by pushing states through the readout directly and ticking the cosmetic clock (a flat shell at rest, 20 rising steps from 0.38 to 0.93, full at ready), while what play has to show is stated as what play can guarantee: five of six sacs slack with at most one still charged.
- 2026-09-01 — **The browser suites come off the deploy path.** Everything that boots Chromium — twenty browser suites and the two collision audits, each playing the built game under software rendering — sat between the build and the publish, so a run needed the better part of an hour of nobody else pushing to reach the deploy step at all. With several sessions on this repository that is not a wait the deploy usually gets: of runs 169–173, three were killed mid-suite by the next push landing (`cancel-in-progress`) and only one was a real failure, so `main` was green while the live site sat hours behind it — and a cancelled run reads exactly like a superseded one, which is what made it hard to see. The deploy now rides the build alone (`npm run build` is `tsc --noEmit && vite build`, so a tree that does not typecheck or does not bundle still cannot publish), which takes a couple of minutes and outruns the next push. The suites run beside it in their own job and report on their own: a red suite marks the run failed and is what says fix or roll back, it no longer holds the site hostage while it does. Deployments queue rather than cancel; a newer commit's suites supersede an older commit's.
- 2026-09-01 — **A dead spider stops standing on its feet, and a corpse is still a body.** Reported after the last change: fitting the rigid solver's contact box to the drawn body fixed the burying, but it also gave a krykna its own flat underside as a resting face, so it settled squarely on its feet every time — which reads as a live animal that has stopped moving. Two changes, and one thing that is simply true. **The box may not be a slab**: a spider measures two and a half times wider than it is deep, and a box that flat has exactly one stable face however it is thrown, so the depth is now floored at a fraction of the body's *narrow* side and raised from the top alone (the bottom face is what the corpse rests on; against the *long* side instead, a low, long massiff got a two-metre box and hung in the air by the difference). Four spiders in six now come to rest on their flank rather than their feet. **A body that would still fall asleep the right way up gets rolled**: a couple across the box, top one way and bottom the other, up to three times — a steady lean does nothing to a shape that is genuinely stable that way up. And **corpses are shootable**: a settled body sits on the scenery team, taking no damage and offering no lock-on, but a bolt moves it about two metres and a blast scatters everything lying nearby. That is the honest answer for the ones that still land flat — a wide flat body resting flat *is* the physical outcome, and nothing short of propping it holds one at an angle, so the player gets to finish the job instead. Both guarantees are now regression-checked.
- 2026-09-02 — **The whole mix went up to the ceiling, evenly.** The game was quiet against other games: the buses ran at master 0.8 with music 0.75 and SFX 0.25 under it, so the score reached the speakers at 0.6 of full scale. Every bus is now scaled by the same 5/3 — master and music at unity, SFX holding exactly the one-third balance under the score it always had — which is the most the mix can give without remixing one sound against another; past this the headroom is in the source files, not the gains. Because summing a score, an ambience bed and a wave's worth of blaster fire at those gains would clip the output, the master bus now leaves through a brick-wall limiter (−1 dB, 20:1, 3 ms attack, 0 dB knee): it catches only what would have distorted — measured at 2–3 dB of reduction in a busy firefight and none in a quiet moment — so the result is louder rather than flatter. Saved volumes are migrated rather than ignored, since a player who had ever touched a slider would otherwise have been the only one not to hear the change: a pre-bump blob is scaled by the same factor on every bus, capped by whatever its loudest bus can still take, so the balance chosen survives exactly and a deliberately quiet mix stays quieter than the default (verified: the old defaults load as 1 / 0.3125 / 0.9375, a quiet 0.3 / 0.1 / 0.2 takes the full 5/3, and a post-bump blob is left alone).
- 2026-09-02 — **The workbench's authored slot shows the model or it shows nothing.** Reported: opening the compare view puts *two procedural figures* side by side. That is what a character is before its .glb lands — the sculpt hides the procedural body when it arrives, so for the second or so before that, both slots are the stand-in — and with nothing saying so it reads as the answer ("they look identical") rather than as a file still in the air. The authored slot now starts hidden behind a progress card and appears when its own model does. The bar is the real byte count: every authored load in the game already reports to the asset tracker in `core/warm.ts`, so the card asks it for this one file's ratio rather than guessing at a duration. Reveal is on the character's own `modelReady`, not the tracker — the tracker calls a file done the moment its bytes land, a beat before the retargeter has rebuilt the skeleton and hidden the body underneath, so revealing on it would flash the very stand-in the card exists to prevent. A file that is genuinely absent is the one case where the stand-in *is* the answer: the card goes, the procedural body appears, and the legend renames the slot to "No model — procedural stand-in" so it still cannot be misread. Props (weapons, the swoop bike, the unrigged massiff) get the card too — they have no stand-in at all, only an empty patch of turntable until the file arrives — and they are no longer built twice: their factories ignore the authored flag, so compare mode had been standing the same sculpt beside itself under two different labels. Two smaller things fell out: a figure behind its card still measures for the camera framing (`visible` is read per mesh, not inherited), which is what stops the camera swinging when the model lands in the space it was holding; and the workbench had been opening in **no clip at all** — `renderPanel` ran once before `spawn`, when there were no figures to ask what they could play, and the "which poses are available" pass quietly moved the pick to the rest pose and left it there. It opens on Idle again. Verified against the real page with the model held back and with the pipe throttled: one card, `Loading din.glb`, 5% → 10% → 14% as the bytes arrive, gone on load with the sculpt in its place; a 404'd model leaves no card and renames the slot; a prop shows one figure and one card.
- 2026-09-02 — **Every territory has a monster now, none of them shared, and the desert has a worm.** An audit of the boss roster against "no monster serves two boards": the six monster finals were already distinct, but the Refinery, the Ringworld and the Prison Rig had no monster at all, and the war massiff was the promoted champion of both the Dune Sea and the Lava Flats. Four creatures fix it, all fan designs from the beasts of the source shows: a **zillo** — the Empire's armored specimen loose in the reactor hall — as the Refinery's final, a **nexu** — a four-eyed quilled hunting cat, the fastest thing on four legs in the game, pouncing like the massiff through a `pounces` flag that generalises the massiff-only gate — on the Ringworld, a **kwazel maw** — a bioluminescent amphibian hauled up out of the moon pool — on the Prison Rig, and the Dune Sea's champion is now a **burrowing worm** in its own right rather than a promoted massiff, which leaves the massiff to Nevarro. **The worm is the set's first bespoke moveset.** `Def.burrows` and `Enemy.updateBurrower` run a cycle — under the sand it hunts at 9.5 m/s as a wake of thrown dust and a sub-bass rumble, untargetable and unhurtable (bolts pass over it, the lock-on finds nothing, damage/knockback/knockdown all return early, and the warlord's anti-camp slam is held while it is down); within 5 m of its prey or after 9 s it rises through boiling sand, erupts for 30 in a 6 m ring with a knock-up and a camera shake out to 24 m, fights rooted for 7 s with the ordinary melee wind-up as its bite, sinks, and hunts again. The body is two things in one character: a `buildMonsterBase` worm for the reared head unit, sunk whole by a new `setBurrow` hook, and three **body arches** — props the game places along a trail of where the root has been, each rising out of and sinking back into the sand on its own beat, so forty metres of animal read whether the head is up or not. The champion promotion passes scale 1 for it, like the finals, so its `Def` is champion-scale by construction (2000 HP under the krayt's 5200). None of the four has a sculpt: they fight as procedural stand-ins (the worm's a reared column of plated coils with three mandibles), their gaits are already written against the node lists their briefs ask for, and the reference canvases and model briefs are open in both asset docs — the pipeline's first step, this time, rather than skipped. The campaign's three monster-less warlord arenas grew to 34×30 for what now comes out of the floor. `tools/test-monsters.mjs` gained three sections: the four stand-ins come up visible at their numbers, each of the three new boards' warlords falls into a quake that becomes its monster by name, and the worm runs its whole cycle — erupts, cannot be hurt under the sand, can be hurt on it, and its head unit measurably goes seven metres under and comes back.

- 2026-09-02 — **The banthas are rideable.** The Dune Sea's livestock is now the game's first living ride (PLAN.md §17). A `living` flag on the vehicle def is what makes a mount different from a machine everywhere it matters: no ignition and no engine loop (it lows when you climb on, grumbles every ten seconds under you, and drops a footfall on the board's surface every half stride, paced off ground covered rather than off a clock), no repulsor bank into a turn — a quarter of it, because the gait carries the roll — no drift, since four feet in the sand bite, and no wreck at the end: it goes down in its own dust with a last low instead of detonating. It *walks*: `bantha.glb` turned out to ride the massiff's Rigify skeleton and ship no animation, so `banthaClips` authors the amble in code alongside the massiff's gaits (long stance, four-beat, a heavy roll onto the planted pair, the horned head nodding once a stride) and the ride blends it against the idle by speed, playing it at the rate that keeps its feet on the ground it is actually covering. Two are parked saddled at the corral's edge; the three grazers behind them are still scenery, and the woven saddle — drawn by the ride, dropped onto whatever back the sculpt measures — is what tells them apart on sight. **X charges**: horns down, 1.75× its own top speed for a second and a half on a five-second cooldown, steering gone heavy for the length of it, and what it hits takes better than twice a normal ram with a longer knockdown for a third of the usual chip. **And your hands are free**: a machine takes both, an animal takes one, so a mounted rider keeps the blaster — RT fires, LT aims, the chest twists to the camera while the gun is up and settles back onto the animal's nose when it comes down, with the blade and the rocket rack stowed as ever (X belongs to the charge). `tools/test-vehicles.mjs` covers the lot: mounting, the seat height off the sculpt, the amble, the charge and its cooldown, a trampled hostile, and aiming from the saddle.

